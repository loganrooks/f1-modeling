{
  "name": "@f1-modeling/visuals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reusable visuals and workbench primitives for the F1 Modeling Lab.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
