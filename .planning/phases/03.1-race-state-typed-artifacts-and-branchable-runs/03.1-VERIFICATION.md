---
phase: 03.1-race-state-typed-artifacts-and-branchable-runs
verified: 2026-04-09T02:33:35Z
status: passed
score: 6/6 must-haves verified
---

# Phase 3.1: Race State, Typed Artifacts, and Branchable Runs — Verification Report

**Phase Goal:** Build the race-state engine, multi-stint scenario contracts, typed timeline artifacts, and run lineage that Phase 4 strategy work requires. This is the architectural bridge between single-stint simulation and race-level analysis.
**Verified:** 2026-04-09T02:33:35Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Scenario schema supports multi-stint race plans with per-stint compound, policy, and lap count | VERIFIED | `raceSchema.ts`: `racePlanSchema` with `stintSpecSchema` (stintIndex, totalLaps, tireCompoundId, electricalPolicyId); `racePlan` field added as optional to `scenarioDocumentSchema` in `schema.ts` |
| 2 | Race-state engine sequences stints with pit events and produces a typed race timeline artifact | VERIFIED | `raceRunner.ts`: `runRace()` sequences stints via `runStint()`/`runStintFromState()`, applies pit events (fresh tires, SoC carry-through), computes `PitLossResult`, emits typed `RaceTimelineEvent` discriminated union sorted chronologically |
| 3 | Race interruptions (VSC, SC, red flag) can be injected at specified laps and affect race state | VERIFIED | `interruptionModel.ts`: `interruptionLapTimeFactor()` (VSC 1.3x, SC 1.4x, red-flag Inf), `pitLossDiscountFactor()` (VSC 0.65, SC 0.4, red-flag 0.0), `applyInterruptionsToLapTimes()` maps race-level laps to stint-local indices; `raceRunner.ts` applies discount at pit-event lap |
| 4 | Run records carry parent/branch lineage so a user can fork from mid-race state and compare alternatives | VERIFIED | `runs/schema.ts`: `parentRunId`, `branchPoint`, `experimentTag` added to `runRecordSchema`; `createRunRecord.ts`: all three fields conditionally spread; API route and `createRaceSimulationRun()` accept and pass through all three |
| 5 | Tire-set inventory tracks allocated and used sets across a race weekend scenario | VERIFIED | `tireInventory.ts`: `validateTireInventory()` enforces per-compound limits and mandatory two-compound rule; `trackTireUsage()` produces ledger entries; `DEFAULT_TIRE_ALLOCATION` (2H/3M/8S); validation runs pre-execution in `createRaceSimulationRun()` |
| 6 | Typed timeline artifacts are structured enough for downstream strategy UI and comparison views | VERIFIED | `types.ts`: 5-variant discriminated union (`StintStartEvent`, `StintEndEvent`, `PitEvent`, `InterruptionStartEvent`, `InterruptionEndEvent`) with full typed fields; `RaceResult` carries `timeline`, `stintSummaries`, `totalRaceTime`, `totalPitTime`; race-timeline artifact persisted to run record |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/domain/src/scenario/raceSchema.ts` | Zod schemas for race domain | VERIFIED | 116 lines; full schemas for stintSpec, interruptionSpec (with endLap > startLap refinement), pitLaneParams, tireSetAllocation, tireInventory, racePlan, branchPoint |
| `packages/sim-core/src/raceModel/types.ts` | Race engine TypeScript interfaces | VERIFIED | 169 lines; PitLaneParams, PitLossResult, InterruptionSpec, StintSpec, RaceConfig, 5-variant RaceTimelineEvent, StintSummary, RaceResult |
| `packages/sim-core/src/raceModel/pitModel.ts` | Pit-lane loss model | VERIFIED | 63 lines; `computePitLoss()` with L_pit formula and discount factor; `DEFAULT_PIT_LANE_PARAMS` (20.5s green-flag delta) |
| `packages/sim-core/src/raceModel/interruptionModel.ts` | Interruption effects model | VERIFIED | 127 lines; `interruptionLapTimeFactor()`, `pitLossDiscountFactor()`, `applyInterruptionsToLapTimes()` with immutability |
| `packages/sim-core/src/raceModel/raceRunner.ts` | Race orchestration engine | VERIFIED | 304 lines; `runRace()` with full multi-stint loop, pit-event state mutation, interruption injection, chronological timeline sort |
| `packages/sim-core/src/raceModel/tireInventory.ts` | Tire inventory validation | VERIFIED | 150 lines; `validateTireInventory()`, `trackTireUsage()`, `DEFAULT_TIRE_ALLOCATION` |
| `packages/domain/src/runs/schema.ts` | Run record with lineage fields | VERIFIED | `parentRunId`, `branchPoint`, `experimentTag` present at lines 75-77 |
| `apps/local-api/src/services/runService.ts` | Race simulation service | VERIFIED | `createRaceSimulationRun()` (~250 lines): scenario load, stint resolution, tire inventory validation, aero config, `runRace()` call, per-stint artifact fan-out, race-timeline artifact, lineage field pass-through |
| `apps/local-api/src/routes/runs.ts` | API route with race-simulation dispatch | VERIFIED | `harnessId` enum includes "race-simulation"; branch dispatches to `createRaceSimulationRun()`; lineage fields in request schema |
| `packages/sim-core/src/raceModel/index.ts` | Race model barrel export | VERIFIED | Exports all types, `runRace`, `computePitLoss`, `DEFAULT_PIT_LANE_PARAMS`, interruption functions, tire inventory functions |
| `packages/sim-core/src/__tests__/pitModel.test.ts` | Pit model unit tests | VERIFIED | 216 lines; 20 assertions covering formula, discount factors, multi-interruption, immutability |
| `packages/sim-core/src/__tests__/tireInventory.test.ts` | Tire inventory unit tests | VERIFIED | 232 lines; 12 assertions covering limits, two-compound rule, wet exemption, prior usage, ledger |
| `packages/sim-core/src/__tests__/raceModel.test.ts` | Race engine integration tests | VERIFIED | 627 lines; 2-stint, 3-stint, state continuity, SC/red-flag scenarios, timeline ordering, plausible values, `runStintFromState` backward compat |
| `apps/local-api/src/__tests__/raceRuns.test.ts` | API integration tests | VERIFIED | 374 lines; 8 tests covering dispatch, artifacts, lineage, 400 on missing racePlan, backward compat |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `scenarioDocumentSchema` | `racePlanSchema` | optional field in `schema.ts` | WIRED | `racePlan: racePlanSchema.optional()` at line 80 |
| `runRecordSchema` | lineage fields | `branchPointSchema` import from raceSchema | WIRED | `parentRunId`, `branchPoint`, `experimentTag` in schema and `createRunRecord` |
| `raceRunner.ts` | `runStint` / `runStintFromState` | stintRunner import | WIRED | Both called in `runRace()` loop |
| `raceRunner.ts` | `computePitLoss` + `applyInterruptionsToLapTimes` | pitModel/interruptionModel imports | WIRED | Both called inside race loop with discount factor lookup |
| `runs.ts` route | `createRaceSimulationRun` | `runService.ts` import | WIRED | Dispatch at `harnessId === "race-simulation"` |
| `createRaceSimulationRun` | `runRace()` | sim-core barrel import | WIRED | Called with `resolvedStints`, `raceConfig`, `interruptions` |
| `createRaceSimulationRun` | `localValidateTireInventory` | inline adapter in `runService.ts` | WIRED | Called pre-execution; throws `RunDependencyError` on failure |
| `createRaceSimulationRun` | `createRunRecord` with lineage | domain import | WIRED | `parentRunId`, `branchPoint`, `experimentTag` conditionally spread |
| domain barrel | all race schemas/types | `packages/domain/src/index.ts` | WIRED | All 7 schemas and 7 types exported from domain barrel |
| sim-core barrel | runRace, pit/interruption/inventory | `packages/sim-core/src/index.ts` | WIRED | All race model functions exported |

### Requirements Coverage

| Requirement | Status | Notes |
|-------------|--------|-------|
| STRA-01 (partial) | SATISFIED | Race plan schema + race engine + interruption injection deliver the partial coverage scoped to this phase |
| STRA-05 (partial) | SATISFIED | Run lineage (parentRunId, branchPoint, experimentTag) enables branch-and-compare workflows |
| VISU-05 (partial) | SATISFIED | Typed timeline artifacts (race-timeline artifact with events, stintSummaries, totalRaceTime, totalPitTime) are structured for downstream strategy UI |

### Anti-Patterns Found

None. Grep across all new raceModel files found zero TODO/FIXME/PLACEHOLDER/stub patterns. No empty returns or console-only handlers.

**Notable pattern:** The local adapter `localValidateTireInventory` in `runService.ts` is explicitly documented as a worktree build workaround with a comment pointing to the canonical implementation. This is not a stub — it is a complete implementation that duplicates the canonical function for a build-system reason, with clear intent to consolidate post-merge.

### Human Verification Required

None. All success criteria are verifiable programmatically:
- Schema structure: inspected source
- Engine logic: inspected source + 344 sim-core tests passing
- API wiring: 34 API tests passing
- Typecheck: clean across all 5 workspaces

## Test Suite Results

- sim-core: **344 tests passing** (16 test files)
- local-api: **34 tests passing** (6 test files)
- domain: **20 tests passing** (2 test files)
- Typecheck: **clean** across all 5 workspaces
- Total: **400 tests, 0 failures**

## Gaps Summary

No gaps. All 6 success criteria are achieved with substantive, wired, tested implementations.

---

_Verified: 2026-04-09T02:33:35Z_
_Verifier: Claude (gsdr-verifier)_
