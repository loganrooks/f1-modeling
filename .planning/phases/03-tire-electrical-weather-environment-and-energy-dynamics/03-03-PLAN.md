---
phase: 03-tire-electrical-weather-environment-and-energy-dynamics
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/sim-core/src/stintModel/environmentModel.ts
  - packages/sim-core/src/stintModel/stintRunner.ts
  - packages/sim-core/src/stintModel/index.ts
  - packages/domain/src/scenario/schema.ts
  - packages/domain/src/index.ts
  - presets/weather/dry-baseline.json
  - presets/weather/light-rain-placeholder.json
  - presets/weather/rain-transition.json
  - apps/local-api/src/services/runService.ts
  - apps/local-api/src/routes/runs.ts
autonomous: true

must_haves:
  truths:
    - "Weather conditions evolve within a session via a deterministic timeline (not only static initial conditions)"
    - "Track temperature, surface wetness, and rubber evolution change per-lap and affect grip"
    - "Environment state modifies effective tire grip through multiplicative factors"
    - "Dry-to-wet and wet-to-dry transitions are representable as weather timeline events"
    - "Rubber evolution builds grip over laps and resets partially with rain"
    - "Stint model runs are available via POST /api/runs with harnessId stint-model"
    - "Phase 2 single-lap runs continue to work via harnessId qss-lap-model"
    - "Scenario schema supports optional stint configuration without breaking existing scenarios"
  artifacts:
    - path: "packages/sim-core/src/stintModel/environmentModel.ts"
      provides: "Weather evolution, surface grip factors, rubber buildup, and environment state updates"
      exports: ["initializeEnvironmentState", "updateEnvironmentState", "surfaceGripFactor", "rubberEvolutionFactor", "interpolateWeatherAtLap"]
    - path: "packages/domain/src/scenario/schema.ts"
      provides: "Optional stintConfig field on scenario document for tire, electrical, and stint parameters"
      contains: "stintConfig"
    - path: "presets/weather/rain-transition.json"
      provides: "Weather preset with evolution timeline for dry-to-wet transition scenario"
    - path: "apps/local-api/src/services/runService.ts"
      provides: "createStintModelRun service function"
      exports: ["createStintModelRun"]
    - path: "apps/local-api/src/routes/runs.ts"
      provides: "stint-model harnessId dispatch in POST /api/runs"
      contains: "stint-model"
  key_links:
    - from: "packages/sim-core/src/stintModel/stintRunner.ts"
      to: "packages/sim-core/src/stintModel/environmentModel.ts"
      via: "updateEnvironmentState() replaces stub in stint loop"
      pattern: "updateEnvironmentState\\("
    - from: "apps/local-api/src/services/runService.ts"
      to: "packages/sim-core/src/stintModel/stintRunner.ts"
      via: "runStint() called from createStintModelRun"
      pattern: "runStint\\("
    - from: "apps/local-api/src/routes/runs.ts"
      to: "apps/local-api/src/services/runService.ts"
      via: "harnessId stint-model dispatches to createStintModelRun"
      pattern: "stint-model.*createStintModelRun"
---

<objective>
Implement dynamic weather evolution and environment coupling, extend the scenario schema for stint configuration, wire the stint model into the API, and create weather presets with evolution timelines.

Purpose: Environment coupling (success criteria 3 and 5) transforms weather from static metadata into a dynamic system that influences tire grip, track conditions, and strategy relevance. API integration makes the stint model accessible from the browser workspace. The scenario schema extension lets users configure stints without breaking existing Phase 2 scenarios.

Output: StintRunner environment stubs replaced with real weather evolution. API serves stint model runs. Scenario schema supports stint configuration. Weather presets include evolution timelines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done-reflect/workflows/execute-plan.md
@$HOME/.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tire-electrical-weather-environment-and-energy-dynamics/03-CONTEXT.md
@.planning/phases/03-tire-electrical-weather-environment-and-energy-dynamics/03-RESEARCH.md
@.planning/phases/03-tire-electrical-weather-environment-and-energy-dynamics/03-01-SUMMARY.md
@packages/sim-core/src/stintModel/types.ts
@packages/sim-core/src/stintModel/stintRunner.ts
@packages/sim-core/src/stintModel/index.ts
@packages/domain/src/scenario/schema.ts
@packages/domain/src/index.ts
@apps/local-api/src/services/runService.ts
@apps/local-api/src/routes/runs.ts
@presets/weather/dry-baseline.json
@presets/weather/light-rain-placeholder.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement environment model, wire into StintRunner, and create weather presets</name>
  <files>
    packages/sim-core/src/stintModel/environmentModel.ts
    packages/sim-core/src/stintModel/stintRunner.ts
    packages/sim-core/src/stintModel/index.ts
    presets/weather/dry-baseline.json
    presets/weather/light-rain-placeholder.json
    presets/weather/rain-transition.json
  </files>
  <action>
    **stintModel/environmentModel.ts** -- Implement weather evolution and environment coupling. Import types from "./types.js".

    1. `interpolateWeatherAtLap(timeline: WeatherTimeline, lap: number): { trackTemperatureC: number; surfaceWetness: number }` -- Linear interpolation between timeline events. For a lap between two events, interpolate both trackTemperatureC and surfaceWetness linearly. Before the first event, use the first event values. After the last event, use the last event values. This implements RESEARCH.md Pitfall 4 mitigation: if rain starts at lap 5.5, lap 5 gets partial rain effect.

    2. `surfaceGripFactor(surfaceWetness: number): number` -- Maps wetness (0.0 = dry, 1.0 = standing water) to a grip multiplier. Dry = 1.0, light wet = 0.8, heavy wet = 0.55. Use a piecewise linear function: `1.0 - 0.45 * surfaceWetness` for wetness 0-1. Floor at 0.55.

    3. `rubberEvolutionFactor(lapNumber: number, surfaceWetness: number, prevRubber: number): number` -- Track rubber builds up over laps improving grip. Starts at 0.95 (green track) and asymptotes toward 1.05 (fully rubbered). Per-lap increment: `0.005 * (1.05 - prevRubber) / 0.10` (diminishing returns). Rain washes rubber away: if surfaceWetness > 0.3, rubber resets partially toward 0.95: `prevRubber - 0.3 * surfaceWetness * (prevRubber - 0.95)`. Clamp to [0.90, 1.05].

    4. `initializeEnvironmentState(timeline: WeatherTimeline): EnvironmentState` -- Creates initial state from the first timeline event (or defaults: trackTemperatureC 33, surfaceWetness 0, rubberEvolution 0.95). Compute gripModifier as `surfaceGripFactor(surfaceWetness) * rubberEvolution`.

    5. `updateEnvironmentState(prev: EnvironmentState, lapNumber: number, timeline: WeatherTimeline): EnvironmentState` -- Per-lap update:
       - Interpolate weather at current lap number to get trackTemperatureC and surfaceWetness.
       - Compute new rubber evolution from `rubberEvolutionFactor(lapNumber, surfaceWetness, prev.rubberEvolution)`.
       - Compute gripModifier as `surfaceGripFactor(surfaceWetness) * rubberEvolution`.
       - Return new EnvironmentState.

    **stintModel/stintRunner.ts** -- Replace environment stub:

    1. Import `initializeEnvironmentState`, `updateEnvironmentState` from "./environmentModel.js".
    2. In `initializeStintState()`: Replace hardcoded environment state with `initializeEnvironmentState(config.weatherTimeline)`.
    3. In the main `runStint()` loop: Replace the environment identity stub with `updateEnvironmentState(state.environmentState, lap, config.weatherTimeline)`.
    4. Remove the "Environment coupling: stub (Plan 03)" assumption. Add: "Weather evolution: deterministic timeline with per-lap interpolation", "Surface grip: multiplicative wetness and rubber factors", "Rubber evolution: buildup with rain washout".
    5. Include environment state snapshots in LapTrace (gripModifier, surfaceWetness, trackTemperatureC).

    **stintModel/index.ts** -- Add re-exports for environmentModel functions.

    **Weather presets** -- Update existing and create new weather presets. All follow the preset document envelope pattern.

    **presets/weather/dry-baseline.json** -- Update values to include evolution parameters:
    ```json
    "values": {
      "surfaceCondition": "dry",
      "rainfall": "none",
      "ambientTemperatureC": 24,
      "trackTemperatureC": 33,
      "weatherTimeline": [
        { "lap": 0, "trackTemperatureC": 33, "surfaceWetness": 0.0, "rainfall": "none" },
        { "lap": 30, "trackTemperatureC": 31, "surfaceWetness": 0.0, "rainfall": "none" }
      ],
      "notes": "Stable dry conditions with slight track temperature drop over stint"
    }
    ```
    Update provenance notes to indicate Phase 3 has added evolution parameters. Keep the existing description structure.

    **presets/weather/light-rain-placeholder.json** -- Rename conceptually from placeholder to light-rain-baseline. Update values:
    ```json
    "values": {
      "surfaceCondition": "damp",
      "rainfall": "light",
      "ambientTemperatureC": 18,
      "trackTemperatureC": 22,
      "weatherTimeline": [
        { "lap": 0, "trackTemperatureC": 22, "surfaceWetness": 0.3, "rainfall": "light" },
        { "lap": 20, "trackTemperatureC": 20, "surfaceWetness": 0.4, "rainfall": "light" },
        { "lap": 40, "trackTemperatureC": 20, "surfaceWetness": 0.3, "rainfall": "light" }
      ],
      "notes": "Consistent light rain with slightly increasing then decreasing wetness"
    }
    ```
    Update presetId to "light-rain-baseline" (keeping file name as light-rain-placeholder.json is acceptable but update the presetId inside). Actually, rename the presetId to "light-rain-baseline" and update the file name to match convention. However, since existing scenarios reference "light-rain-placeholder", keep the presetId as "light-rain-placeholder" for backward compatibility but update all other fields.

    **presets/weather/rain-transition.json** -- NEW preset for dry-to-wet scenario:
    ```json
    {
      "schemaVersion": "preset-document/v1",
      "presetId": "rain-transition",
      "presetType": "weather",
      "name": "Rain Transition",
      "description": "Dynamic weather scenario: dry start, rain onset at lap 15, full wet by lap 20, drying from lap 35. Educational model for exploring weather-driven strategy decisions.",
      "provenance": {
        "sourceType": "engineering-inference",
        "source": "Phase 3 weather evolution model",
        "notes": "Authored scenario for weather strategy exploration. Not a meteorological prediction."
      },
      "assumptionNotes": [
        {
          "note": "Weather timeline is a deterministic authored input, not a weather prediction. Transitions are linear interpolations at lap boundaries.",
          "provenance": {
            "sourceType": "engineering-inference",
            "source": "Phase 3 weather model design",
            "notes": "See guardrail 3 in CONTEXT.md"
          }
        }
      ],
      "values": {
        "surfaceCondition": "variable",
        "rainfall": "variable",
        "ambientTemperatureC": 22,
        "trackTemperatureC": 28,
        "weatherTimeline": [
          { "lap": 0, "trackTemperatureC": 28, "surfaceWetness": 0.0, "rainfall": "none" },
          { "lap": 14, "trackTemperatureC": 25, "surfaceWetness": 0.0, "rainfall": "none" },
          { "lap": 15, "trackTemperatureC": 22, "surfaceWetness": 0.2, "rainfall": "light" },
          { "lap": 20, "trackTemperatureC": 18, "surfaceWetness": 0.7, "rainfall": "heavy" },
          { "lap": 35, "trackTemperatureC": 18, "surfaceWetness": 0.7, "rainfall": "heavy" },
          { "lap": 45, "trackTemperatureC": 22, "surfaceWetness": 0.2, "rainfall": "light" },
          { "lap": 55, "trackTemperatureC": 25, "surfaceWetness": 0.0, "rainfall": "none" }
        ],
        "notes": "Dynamic transition scenario for exploring tire compound and pit timing strategy under changing conditions"
      }
    }
    ```
  </action>
  <verify>
    - `npm run typecheck` passes in packages/sim-core
    - `grep "updateEnvironmentState\|surfaceGripFactor\|rubberEvolutionFactor" packages/sim-core/src/stintModel/environmentModel.ts` shows all expected exports
    - `grep "updateEnvironmentState" packages/sim-core/src/stintModel/stintRunner.ts` confirms real environment update (not stub)
    - No `// Phase 3 Plan 03` stub comments remain in stintRunner.ts
    - All three weather preset files exist and contain weatherTimeline arrays
    - `cat presets/weather/rain-transition.json | grep "weatherTimeline"` confirms new preset
  </verify>
  <done>
    Environment model implements weather evolution (track temperature, surface wetness, rubber buildup) as per-lap multiplicative grip modifiers with linear interpolation between timeline events. StintRunner environment stubs fully replaced. Three weather presets with evolution timelines: dry baseline, light rain, and rain transition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend scenario schema with stint config and wire stint model into API</name>
  <files>
    packages/domain/src/scenario/schema.ts
    packages/domain/src/index.ts
    apps/local-api/src/services/runService.ts
    apps/local-api/src/routes/runs.ts
  </files>
  <action>
    **packages/domain/src/scenario/schema.ts** -- Add optional stint configuration to the scenario schema:

    1. Add `stintConfigSchema` as an optional sub-object:
    ```typescript
    export const stintConfigSchema = z.object({
      totalLaps: z.number().int().positive(),
      tireCompoundId: documentIdSchema,                  // References a tire compound preset
      electricalPolicyId: z.string().optional(),         // "aggressive-deploy" | "conservative" | custom
      weatherPresetOverride: documentIdSchema.optional(), // Override the scenario-level weather preset for stint
    }).strict();
    ```

    2. Add `stintConfig: stintConfigSchema.optional()` to `scenarioDocumentSchema`. This MUST be optional so all existing scenarios continue to validate. Place it after `vehicleParams` and before `observerConfigPlaceholder`.

    3. Export the new schema and its type: `export type StintConfig = z.infer<typeof stintConfigSchema>;`.

    **packages/domain/src/index.ts** -- Ensure `stintConfigSchema` and `StintConfig` type are re-exported if the domain index currently re-exports scenario types.

    **apps/local-api/src/services/runService.ts** -- Add `createStintModelRun`:

    1. Import `runStint`, `DEFAULT_VEHICLE_PARAMS`, `DEFAULT_LOAD_TRANSFER_PARAMS` from "@f1-modeling/sim-core". Also import the necessary stint model types (StintConfig, TireCompoundParams, etc.).
    2. Import tire compound preset loading: create a helper or inline that reads tire compound JSON from the presets/tires/ directory (follow the same pattern as circuit catalog loading -- use `loadPresetDocument` or read JSON directly from `join(paths.presetsRoot, 'tires', compoundId + '.json')`).
    3. Import weather preset with timeline: read the weather preset values.weatherTimeline array from the resolved weather preset.

    4. `createStintModelRun(paths, input)`:
       - Load scenario (same pattern as createLapModelRun).
       - Resolve presets.
       - Load circuit.
       - Load tire compound preset from `scenario.stintConfig.tireCompoundId` (default to "medium-c3" if not specified).
       - Build StintConfig: circuit, baseVehicle (from scenario or defaults), tireCompound (from preset values), electricalPolicy (from policyId or default conservative), weatherTimeline (from weather preset values.weatherTimeline or default dry timeline), aeroConfig (from regulation preset values.aero if activeAeroModeSwitching is true, else null), loadTransferParams (DEFAULT_LOAD_TRANSFER_PARAMS), totalLaps (from stintConfig or default 15).
       - For aeroConfig: build from regulation preset values. cornerModeDragFactor = baseVehicle.dragFactor. straightModeDragFactor = cornerModeDragFactor * (1 - dragReduction). Same for downforce. straightModeZones: for now, use a simple heuristic -- define one straight zone per circuit by finding the longest contiguous section with curvature near zero (|curvature| < 0.001). This is a reasonable heuristic; exact DRS/active-aero zone definitions are circuit-specific and would come from circuit metadata in a future phase.
       - Call `runStint(stintConfig)`.
       - Build run record with summaryMetrics including harnessId: "stint-model", lapTimes, totalTime, tireCompound, electricalPolicy, totalLaps. Include per-lap stint trace as an artifact with artifactType "stint-trace".
       - Include tire-degradation-trace artifact (lap number vs wear/grip/temp).
       - Include electrical-state-trace artifact (lap number vs SoC/deployed/harvested).
       - Include weather-evolution-trace artifact (lap number vs trackTemp/wetness/gripModifier).
       - Persist and return.

    **apps/local-api/src/routes/runs.ts** -- Add stint-model to the harness dispatch:

    1. Update `createRunRequestSchema`: extend harnessId enum to include "stint-model": `z.enum(["qss-lap-model", "phase1-placeholder", "stint-model"]).optional()`.
    2. Add dispatch case: `if (body.harnessId === "stint-model") { runRecord = await createStintModelRun(paths, body); }`.
    3. Import `createStintModelRun` from the run service.
    4. The fallback behavior (no harnessId) should NOT default to stint-model -- keep existing fallback (try lap model, then placeholder). Stint model requires explicit harnessId selection.
  </action>
  <verify>
    - `npm run typecheck` passes across all workspaces (especially apps/web which imports domain types)
    - `npm run test` passes -- existing Phase 2 API tests must remain green
    - `grep "stintConfig" packages/domain/src/scenario/schema.ts` confirms optional stint config field
    - `grep "stint-model" apps/local-api/src/routes/runs.ts` confirms new harnessId dispatch
    - `grep "createStintModelRun" apps/local-api/src/services/runService.ts` confirms new service function
    - Existing scenarios (without stintConfig) still validate against the updated schema
  </verify>
  <done>
    Scenario schema extended with optional stintConfig (totalLaps, tireCompoundId, electricalPolicyId). API dispatches stint-model harnessId to createStintModelRun. Run records include stint-trace, tire-degradation-trace, electrical-state-trace, and weather-evolution-trace artifacts. Phase 2 single-lap runs via qss-lap-model continue to work. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes across all workspaces
- `npm run test` passes -- no Phase 2 regressions
- POST /api/runs with harnessId "stint-model" produces a run record with stint traces
- POST /api/runs with harnessId "qss-lap-model" still works (Phase 2 backward compatibility)
- Weather presets include weatherTimeline arrays
- Scenario schema accepts stintConfig but does not require it
- StintRunner has no remaining stub comments for environment
</verification>

<success_criteria>
- Weather conditions evolve within a session via deterministic timeline
- Environment coupling modifies tire grip through multiplicative factors
- Rain transitions are representable and produce observable grip changes
- API serves stint model runs at POST /api/runs
- Phase 2 runs remain fully functional
- Scenario schema is backward compatible
</success_criteria>

<output>
After completion, create `.planning/phases/03-tire-electrical-weather-environment-and-energy-dynamics/03-03-SUMMARY.md`
</output>
