---
phase: 03.2-backend-execution-job-artifacts-and-regulation-execution-flow
plan: 03
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - apps/local-api/src/app.ts
  - apps/local-api/src/persistence/fileStore.ts
  - apps/local-api/src/persistence/artifactStore.ts
  - apps/local-api/src/services/runReadModelService.ts
  - apps/local-api/src/services/runService.ts
  - apps/local-api/src/__tests__/artifactStore.test.ts
  - apps/local-api/src/__tests__/runReadModel.test.ts
  - packages/domain/src/common/schemaVersion.ts
  - packages/domain/src/runs/schema.ts
  - packages/domain/src/runs/createRunRecord.ts
  - packages/domain/src/index.ts
autonomous: true
planning_posture:
  context_mode: full_context
  context_path: .planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-CONTEXT.md
  canonical_refs_resolved: true
future_preservation:
  - source_bucket: Protected Seams
    item: "Preserve a first-class artifact identity model with lineage, provenance, fidelity, validation, and regulation references."
    disposition: preserved_seam
    plan_consequence: "Persist artifact envelopes independently from run records and keep manifest entries keyed by artifactId plus artifactKey instead of treating inline JSON blobs as the canonical artifact surface."
  - source_bucket: Protected Seams
    item: "Preserve a distinction between run lineage, job identity, artifact identity, and future branch/replay state."
    disposition: preserved_seam
    plan_consequence: "Stored run records must reference artifacts through manifest entries and keep room for job metadata without overloading runId or artifactId."
  - source_bucket: Protected Seams
    item: "Preserve the raw regulation document -> canonical semantic form -> execution snapshot pipeline."
    disposition: sequencing_choice
    plan_consequence: "Artifact and stored-run provenance must reserve raw-source and execution-snapshot reference slots now so 03.2-05 can populate them without another schema reshape."
  - source_bucket: Explicit Non-Decisions
    item: "Do not decide the full D2 anchor/addressability model; only preserve the artifact identity seams D2 and D3 will need."
    disposition: non_action_rationale
    plan_consequence: "Include only reserved addressability fields such as anchor namespace or payload schema identity. Do not define AnchorRegistry, anchor resolution states, or recipe bindings here."
  - source_bucket: Explicit Non-Decisions
    item: "Do not widen this phase into a user-facing shell rewrite or lesson-delivery phase."
    disposition: non_action_rationale
    plan_consequence: "Preserve `GET /api/runs` as a compatibility read model instead of attempting a new Phase 3.3-era UI data flow in this storage plan."
  - source_bucket: Current Posture
    item: "Existing saved runs and local persistence remain part of the product contract and cannot be discarded casually during artifact migration."
    disposition: validation_task
    plan_consequence: "Introduce read compatibility for `run-record/v1`, write a new stored form for new runs, and test both old and new persisted shapes through the same listing/materialization path."
  - source_bucket: Current Posture
    item: "Local-first and single-user remain the active operating posture."
    disposition: preserved_seam
    plan_consequence: "Persist artifact envelopes and stored runs as local JSON documents under `workspace-data/` rather than adding broker, database, or cloud dependencies."
  - source_bucket: Future Shape Notes
    item: "Phase 3.3 will need stable artifact identity, partial/final status, and visible provenance/fidelity hooks."
    disposition: preserved_seam
    plan_consequence: "Artifact manifest entries and envelopes must include status, revision, fidelity tier, validation state, and regulation reference fields even though the current web still uses compatibility materialization."
  - source_bucket: Future Shape Notes
    item: "Phase 4.1 replay and observer work will need durable artifact fetch and clearer separation between execution events and read-model surfaces."
    disposition: preserved_seam
    plan_consequence: "Independent artifact fetch must become canonical now, while `GET /api/runs` remains only a compatibility read surface."
tech_debt_disposition:
  - id: VA-TD-01
    status: intentionally_deferred
    note: "This plan introduces the persistence substrate the split will use, but it does not yet break apart runService orchestration."
  - id: VA-TD-02
    status: intentionally_deferred
    note: "This plan reserves regulation provenance slots in stored runs and artifact envelopes, but the actual execution snapshot compilation lands in 03.2-05."
  - id: VA-TD-03
    status: intentionally_deferred
    note: "Artifact durability is a prerequisite for async jobs, but the authoritative backend swap and job surface land in 03.2-04 and 03.2-02."
must_haves:
  truths:
    - "ArtifactEnvelope instances are stored independently from run records and can be fetched by artifactId."
    - "Stored runs no longer need inline `artifacts[].data` blobs as their canonical persisted form."
    - "The repo can still read historical `run-record/v1` files after the migration."
    - "GET-style run read models can still materialize the inline artifact data the current web panels consume."
    - "Artifact manifests preserve artifactId, artifactKey, status, revision, lineage, and provenance references needed by later phases."
  artifacts:
    - path: "apps/local-api/src/persistence/artifactStore.ts"
      provides: "File-backed artifact envelope persistence and fetch helpers."
      exports: ["writeArtifactEnvelope", "readArtifactEnvelope", "listArtifactManifestEntries"]
    - path: "apps/local-api/src/services/runReadModelService.ts"
      provides: "Compatibility materialization from stored run records plus artifact envelopes into the current web-facing RunRecord shape."
      exports: ["materializeRunReadModel", "listStoredRuns"]
    - path: "packages/domain/src/runs/schema.ts"
      provides: "Read compatibility for stored run-record versions and manifest-backed run schemas."
      exports: ["storedRunRecordSchema", "runRecordSchema"]
  key_links:
    - from: "apps/local-api/src/services/runService.ts"
      to: "apps/local-api/src/persistence/artifactStore.ts"
      via: "current synchronous run paths persist artifacts through the new store instead of embedding canonical payloads in run JSON"
      pattern: "writeArtifactEnvelope"
    - from: "apps/local-api/src/services/runReadModelService.ts"
      to: "packages/domain/src/runs/schema.ts"
      via: "materialized read models stay within the existing RunRecord contract while stored records evolve"
      pattern: "materializeRunReadModel"
    - from: "packages/domain/src/runs/schema.ts"
      to: "packages/domain/src/execution/artifacts.ts"
      via: "stored run manifests reference the D1 artifact identity model rather than ad hoc artifact blobs"
      pattern: "artifactKey"
---

<objective>
Persist durable artifact envelopes and migrate stored run records without breaking the current web read model.

Purpose: Give Phase 3.2 a real artifact substrate before public job routes depend on it. This plan makes independent artifact fetch canonical while preserving the existing `/api/runs` shape as a compatibility surface.
Output: File-backed artifact store, manifest-backed stored run records, and a materialized read-model service that keeps current web consumers working.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/LONG-ARC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TECH-DEBT.md
@.planning/initiatives/vision-alignment-2026-04/deliberations/01-decision-anchor.md
@.planning/initiatives/vision-alignment-2026-04/deliberations/01-backend-boundary-architecture.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-CONTEXT.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-RESEARCH.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-01-SUMMARY.md
@apps/local-api/src/app.ts
@apps/local-api/src/persistence/fileStore.ts
@apps/local-api/src/services/runService.ts
@apps/web/src/features/workspace/api.ts
@apps/web/src/features/runs/RunSummaryPanel.tsx
@packages/domain/src/runs/schema.ts
@packages/domain/src/runs/createRunRecord.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a file-backed artifact store and manifest persistence layer</name>
  <files>
    apps/local-api/src/app.ts
    apps/local-api/src/persistence/fileStore.ts
    apps/local-api/src/persistence/artifactStore.ts
    apps/local-api/src/__tests__/artifactStore.test.ts
  </files>
  <action>
Introduce canonical artifact storage under `workspace-data/artifacts/`.

1. Extend `LocalApiPaths` in `apps/local-api/src/app.ts` with the directories this phase now needs at minimum:
   - `artifactsDir`
   - any sibling directories the later job store will need, if adding them now simplifies path ownership

2. Create `apps/local-api/src/persistence/artifactStore.ts` with helpers that operate on `ArtifactEnvelope` and manifest entries:
   - `writeArtifactEnvelope(paths, envelope)`
   - `readArtifactEnvelope(paths, artifactId)`
   - `listArtifactManifestEntries(paths, query)`
   - any digest or revision helpers needed to keep artifact ids and revisions stable

3. Keep the storage intentionally local-first:
   - artifact envelopes written as normalized JSON
   - one persisted instance per `artifactId`
   - manifests keyed by `artifactKey`, `runId`, and `runFamilyId`
   - no database, no queue broker, no remote blob store

4. If `apps/local-api/src/persistence/fileStore.ts` needs helper upgrades, keep them generic and narrowly scoped to:
   - deterministic JSON writes
   - directory enumeration
   - no artifact-specific logic in the generic file store

5. Add `apps/local-api/src/__tests__/artifactStore.test.ts` covering:
   - independent artifact persistence
   - list-by-run and list-by-runFamily queries
   - overwrite/revision safety
   - missing artifact behavior
  </action>
  <verify>
Run `npm run typecheck --workspace apps/local-api`.
Run `npm run test --workspace apps/local-api -- artifactStore`.
Manually inspect one written artifact JSON to confirm it stores envelope metadata plus payload separately from run JSON.
  </verify>
  <done>
Artifact envelopes can be stored and fetched independently of run records, and the store stays file-backed and local-first.
  </done>
</task>

<task type="auto">
  <name>Task 2: Introduce stored-run compatibility and materialized read models</name>
  <files>
    packages/domain/src/common/schemaVersion.ts
    packages/domain/src/runs/schema.ts
    packages/domain/src/runs/createRunRecord.ts
    packages/domain/src/index.ts
    apps/local-api/src/services/runReadModelService.ts
    apps/local-api/src/services/runService.ts
    apps/local-api/src/__tests__/runReadModel.test.ts
  </files>
  <action>
Migrate the stored run shape without breaking current consumers.

1. In `packages/domain/src/runs/schema.ts`, separate canonical stored-run concerns from the compatibility read model:
   - keep explicit support for historical `run-record/v1`
   - add a manifest-backed stored form for newly written runs
   - preserve `runRecordSchema` as the current compatibility read-model contract used by the web client
   - add explicit exports for the stored-run schema(s) so local-api can parse on-disk documents honestly

2. In `packages/domain/src/runs/createRunRecord.ts`, add or split helpers so local-api can:
   - create stored run records for disk
   - materialize compatibility `RunRecord` objects for API responses
   Keep lineage, preset snapshots, and assumption-note behavior intact.

3. Create `apps/local-api/src/services/runReadModelService.ts` that:
   - reads historical v1 runs directly
   - reads new manifest-backed stored runs
   - resolves only the current web-needed artifact payloads back into the compatibility `run.artifacts[].data` shape
   - avoids building a second canonical artifact model inside the read service

4. Update `apps/local-api/src/services/runService.ts` only enough to route current synchronous write paths through:
   - the new artifact store
   - the new stored-run write helper
   - the new read-model materializer
   Do not perform the larger orchestration split yet; that belongs to 03.2-04.

5. Add `apps/local-api/src/__tests__/runReadModel.test.ts` covering:
   - historical `run-record/v1` compatibility
   - new stored runs materializing to the current web-facing `RunRecord`
   - inline artifact data still available for the artifact types current web panels consume
   - no destructive migration requirement on old files
  </action>
  <verify>
Run `npm run typecheck --workspace packages/domain`.
Run `npm run test --workspace packages/domain`.
Run `npm run test --workspace apps/local-api -- runReadModel`.
Manually confirm that `GET /api/runs` can still provide a web-compatible run object after a new run is written through the new storage path.
  </verify>
  <done>
New runs are stored with manifest-backed artifact references, old runs remain readable, and the current web UI can continue consuming a materialized compatibility read model while D1 storage becomes canonical underneath.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck --workspace packages/domain`
- [ ] `npm run test --workspace packages/domain`
- [ ] `npm run typecheck --workspace apps/local-api`
- [ ] `npm run test --workspace apps/local-api -- artifactStore`
- [ ] `npm run test --workspace apps/local-api -- runReadModel`
- [ ] Historical `run-record/v1` files still load and new runs write separate artifact envelopes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Durable artifact fetch is canonical underneath the API surface
- Stored runs no longer depend on inline artifact payloads as the canonical on-disk form
- Current web consumers still receive the run shape they need through a compatibility materialization layer
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-03-SUMMARY.md`
</output>
