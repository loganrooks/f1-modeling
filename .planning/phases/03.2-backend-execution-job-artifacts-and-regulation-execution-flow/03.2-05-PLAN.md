---
phase: 03.2-backend-execution-job-artifacts-and-regulation-execution-flow
plan: 05
type: execute
wave: 4
depends_on: ["03.2-01", "03.2-03", "03.2-04"]
files_modified:
  - presets/regulations/fia-2026-baseline.json
  - packages/domain/src/execution/artifacts.ts
  - packages/domain/src/execution/jobs.ts
  - packages/domain/src/execution/regulation.ts
  - packages/domain/src/runs/schema.ts
  - packages/domain/src/__tests__/documents.test.ts
  - apps/local-api/src/services/regulationCompiler.ts
  - apps/local-api/src/services/runCompiler.ts
  - apps/local-api/src/services/localSimulationBackend.ts
  - apps/local-api/src/services/runExecutionService.ts
  - apps/local-api/src/services/runPersistenceService.ts
  - apps/local-api/src/persistence/artifactStore.ts
  - apps/local-api/src/persistence/jobStore.ts
  - apps/local-api/src/services/jobService.ts
  - apps/local-api/src/workers/localSimulationWorker.ts
  - apps/local-api/src/__tests__/runCompiler.test.ts
  - apps/local-api/src/__tests__/regulationCompiler.test.ts
  - apps/local-api/src/__tests__/localSimulationBackend.test.ts
  - apps/local-api/src/__tests__/runCompatibilityRoutes.test.ts
  - apps/local-api/src/__tests__/jobRoutes.test.ts
  - packages/sim-core/src/stintModel/types.ts
  - packages/sim-core/src/stintModel/electricalModel.ts
  - packages/sim-core/src/stintModel/stintRunner.ts
  - packages/sim-core/src/raceModel/types.ts
  - packages/sim-core/src/raceModel/raceRunner.ts
  - packages/sim-core/src/__tests__/electricalModel.test.ts
  - packages/sim-core/src/__tests__/raceModel.test.ts
autonomous: true
planning_posture:
  context_mode: full_context
  context_path: .planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-CONTEXT.md
  canonical_refs_resolved: true
future_preservation:
  - source_bucket: Protected Seams
    item: "Preserve regulation-family-aware execution and semantics; do not let 2026-specific assumptions harden into the long-lived runtime path."
    disposition: preserved_seam
    plan_consequence: "Compile an ExecutionRegulationSnapshot with family identity, override lineage, canonical semantic reference, stable semantic identifiers, and explicit capability modules, then thread that snapshot through sim-core instead of reading 2026 constants or raw preset casts at runtime."
  - source_bucket: Protected Seams
    item: "Preserve the raw regulation document -> canonical semantic form -> execution snapshot pipeline."
    disposition: preserved_seam
    plan_consequence: "Store raw preset provenance and compiled execution snapshot hashes together, but keep the compiler as the only place allowed to translate raw authored regulation documents into runtime inputs."
  - source_bucket: Protected Seams
    item: "Preserve a first-class artifact identity model with lineage, provenance, fidelity, validation, and regulation references."
    disposition: validation_task
    plan_consequence: "Execution-time artifacts and stored runs must record source preset ids, family/variant identity, override lineage, canonical semantic reference, stable semantic identifiers, execution snapshot hash, and compiler version so later comparison and lesson phases inherit honest provenance."
  - source_bucket: Explicit Non-Decisions
    item: "Do not decide the full cross-family comparability/applicability registry beyond the execution-facing slice needed now."
    disposition: non_action_rationale
    plan_consequence: "Populate the execution-facing family identity, override lineage, canonical semantic reference, stable semantic identifiers, and reserved applicability/provenance slots. Do not attempt D3/D5 registry or relation-class UI work here."
  - source_bucket: Explicit Non-Decisions
    item: "Do not decide yet whether future remote or cloud compute is self-hosted only, partnered, or fully hosted."
    disposition: non_action_rationale
    plan_consequence: "Regulation compilation stays a pre-backend concern and does not assume anything about backend location."
  - source_bucket: Explicit Non-Decisions
    item: "Do not widen this phase into a user-facing shell rewrite or lesson-delivery phase."
    disposition: non_action_rationale
    plan_consequence: "This plan only changes compiler and runtime inputs plus provenance; it does not surface new semantic UI or lesson features."
  - source_bucket: Current Posture
    item: "Reduced-order, explicit-about-limits execution remains the governing modeling posture; async jobs must not imply calibrated or higher-fidelity physics."
    disposition: preserved_seam
    plan_consequence: "The regulation snapshot parameterizes the current reduced-order kernels and unsupported capability combinations must fail honestly instead of being silently approximated."
  - source_bucket: Current Posture
    item: "Existing saved runs and local persistence remain part of the product contract and cannot be discarded casually during artifact migration."
    disposition: validation_task
    plan_consequence: "Stored runs must retain raw preset snapshots while adding compiled execution provenance so historical reproducibility improves rather than regresses."
  - source_bucket: Future Shape Notes
    item: "Phase 3.4 will need regulation family identity, variant lineage, and semantic applicability slots preserved through artifacts and execution snapshots."
    disposition: preserved_seam
    plan_consequence: "Include family id, optional family variant id, override lineage, canonical semantic reference, stable semantic identifiers, and reserved semantic/applicability references in the execution snapshot and artifact provenance now."
  - source_bucket: Future Shape Notes
    item: "Later import/calibration work will need stored raw-plus-compiled regulation lineage and fetchable artifacts."
    disposition: preserved_seam
    plan_consequence: "Raw preset snapshots remain stored, compiled snapshots get stable hashes, and runtime artifacts reference both."
tech_debt_disposition:
  - id: VA-TD-01
    status: not_applicable
    note: "The runService split was paid in 03.2-04; this plan consumes that seam but does not reopen it."
  - id: VA-TD-02
    status: retired
    note: "This is the plan that compiles canonical execution snapshots and removes raw-preset plus hardcoded-2026 assumptions from the runtime path."
  - id: VA-TD-03
    status: not_applicable
    note: "The async backend contract is already in place by the time this regulation cleanup runs; this plan focuses on what crosses that boundary."
must_haves:
  truths:
    - "ExecutionRegulationSnapshot is compiled from raw regulation presets before backend submission."
    - "ExecutionRegulationSnapshot preserves family/variant identity, override lineage, a canonical semantic reference, and stable semantic identifiers required by the D5 handshake."
    - "The runtime path no longer depends on hardcoded 2026 electrical constants in electricalModel.ts."
    - "Active aero execution inputs are derived from the compiled snapshot rather than raw preset casts inside runService.ts."
    - "Race execution checks an explicit refueling capability from the snapshot instead of relying on an implicit '2026 means no refueling' assumption."
    - "Stored run and artifact provenance carry raw-source identity plus compiled execution-snapshot identity, override lineage, canonical semantic reference, and stable semantic identifiers for reproducibility."
  artifacts:
    - path: "apps/local-api/src/services/regulationCompiler.ts"
      provides: "The only compiler that turns raw regulation presets into ExecutionRegulationSnapshot values."
      exports: ["compileExecutionRegulationSnapshot"]
    - path: "packages/domain/src/execution/regulation.ts"
      provides: "Execution snapshot schema with family identity, override lineage, canonical semantic reference, stable semantic identifiers, and capability modules."
      exports: ["executionRegulationSnapshotSchema", "ExecutionRegulationSnapshot"]
    - path: "apps/local-api/src/services/runExecutionService.ts"
      provides: "Backend-facing execution orchestration that persists compiled regulation provenance through job completion and stored-run creation."
      exports: ["submitCompiledRequest", "awaitJobCompletion"]
    - path: "apps/local-api/src/services/runPersistenceService.ts"
      provides: "Stored-run and artifact persistence helpers that carry raw-plus-compiled regulation lineage into durable records and compatibility read models."
      exports: ["persistCompletedRun", "materializeRunReadModel"]
    - path: "packages/domain/src/execution/artifacts.ts"
      provides: "Typed artifact-envelope provenance fields for raw-plus-compiled regulation lineage, including semantic identifiers and canonical semantic reference."
      exports: ["artifactEnvelopeSchema", "artifactRefSchema"]
    - path: "packages/domain/src/execution/jobs.ts"
      provides: "Typed job snapshot and event schemas that can carry execution-regulation identity through terminal job state."
      exports: ["jobSnapshotSchema", "jobEventSchema"]
    - path: "packages/domain/src/runs/schema.ts"
      provides: "Stored-run and compatibility-run schemas with explicit raw-plus-compiled regulation lineage fields."
      exports: ["storedRunRecordSchema", "runRecordSchema"]
    - path: "packages/sim-core/src/stintModel/electricalModel.ts"
      provides: "Electrical limits and initialization driven by compiled regulation capabilities rather than hardcoded 2026 constants."
      contains: "maxDeployPowerW|maxHarvestPerLapJ|maxDeltaSoCJ"
    - path: "packages/sim-core/src/raceModel/raceRunner.ts"
      provides: "Race execution behavior that reads refueling capability from the compiled regulation snapshot."
      contains: "refueling"
  key_links:
    - from: "apps/local-api/src/services/runCompiler.ts"
      to: "apps/local-api/src/services/regulationCompiler.ts"
      via: "compiled requests receive an ExecutionRegulationSnapshot before backend submission"
      pattern: "compileExecutionRegulationSnapshot"
    - from: "apps/local-api/src/services/regulationCompiler.ts"
      to: "packages/domain/src/execution/regulation.ts"
      via: "compiler output is parsed through the domain execution-regulation schema"
      pattern: "executionRegulationSnapshotSchema"
    - from: "apps/local-api/src/services/runExecutionService.ts"
      to: "apps/local-api/src/services/runPersistenceService.ts"
      via: "compiled regulation provenance survives worker execution and is written into stored runs and compatibility read models"
      pattern: "persistCompletedRun|materializeRunReadModel"
    - from: "apps/local-api/src/services/jobService.ts"
      to: "packages/domain/src/execution/jobs.ts"
      via: "terminal job snapshots and replayed job events preserve execution-regulation identity in typed route-facing payloads"
      pattern: "jobSnapshotSchema|jobEventSchema"
    - from: "packages/sim-core/src/stintModel/electricalModel.ts"
      to: "packages/domain/src/execution/regulation.ts"
      via: "runtime electrical limits come from the compiled electrical capability module"
      pattern: "maxDeployPowerW|maxHarvestPerLapJ|maxDeltaSoCJ"
---

<objective>
Compile canonical execution regulation snapshots and remove raw-preset plus 2026-specific runtime assumptions from the execution path.

Purpose: Close the regulation side of the D1 corridor honestly so later phases inherit family-aware execution provenance instead of a boundary that still means '2026 by implication.'
Output: Regulation compiler, snapshot-threaded runtime inputs, and provenance updates proving compute used a compiled execution snapshot rather than raw preset maps.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/LONG-ARC.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/TECH-DEBT.md
@.planning/initiatives/vision-alignment-2026-04/deliberations/01-decision-anchor.md
@.planning/initiatives/vision-alignment-2026-04/deliberations/01-backend-boundary-architecture.md
@.planning/initiatives/vision-alignment-2026-04/deliberations/04-decision-anchor.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-CONTEXT.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-RESEARCH.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-01-SUMMARY.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-03-SUMMARY.md
@.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-04-SUMMARY.md
@presets/regulations/fia-2026-baseline.json
@apps/local-api/src/services/runCompiler.ts
@packages/domain/src/presets/schema.ts
@packages/sim-core/src/stintModel/electricalModel.ts
@packages/sim-core/src/stintModel/types.ts
@packages/sim-core/src/raceModel/raceRunner.ts
@packages/sim-core/src/raceModel/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a regulation compiler and explicit execution capability data</name>
  <files>
    presets/regulations/fia-2026-baseline.json
    packages/domain/src/execution/regulation.ts
    apps/local-api/src/services/regulationCompiler.ts
    apps/local-api/src/services/runCompiler.ts
    apps/local-api/src/__tests__/runCompiler.test.ts
    apps/local-api/src/__tests__/regulationCompiler.test.ts
  </files>
  <action>
Make execution regulation compilation explicit and testable.

1. Update the 2026 baseline regulation preset only as much as needed to support honest compilation:
   - preserve backward readability of the authored document
   - add explicit family identity and any capability data the compiler would otherwise have to invent implicitly
   - do not widen the raw preset into the full D5 registry or authoring grammar

2. Create `apps/local-api/src/services/regulationCompiler.ts` implementing:
   - `compileExecutionRegulationSnapshot(regulationPreset)`
   - stable source snapshot hashing
   - execution snapshot hashing
   - compiler version tagging
   - stable semantic identifiers for comparison-relevant concepts
   - override-lineage capture and canonical semantic reference emission
   - capability extraction for `aero`, `electrical`, `refueling`, and any current tire-usage/session constraint slice needed by execution

3. Update `apps/local-api/src/services/runCompiler.ts` so every compiled request gets its `ExecutionRegulationSnapshot` from the new compiler service, not from raw `regulation.values` casts or route-local defaults.

4. Update the typed schema layer so the new provenance promises have an explicit contract home:
   - extend `packages/domain/src/execution/artifacts.ts` so artifact envelopes can carry raw-plus-compiled regulation lineage, including canonical semantic reference and stable semantic identifiers
   - extend `packages/domain/src/execution/jobs.ts` so terminal job snapshots and relevant events can carry execution-regulation identity honestly
   - extend `packages/domain/src/runs/schema.ts` so stored runs and compatibility read models have explicit fields for raw-plus-compiled regulation lineage rather than relying on ad hoc persistence payloads
   - update `packages/domain/src/__tests__/documents.test.ts` so these schema changes are parsed and versioned deliberately

5. Keep this split explicit:
   - raw preset document stays stored for authored reproducibility
   - compiled snapshot becomes the only runtime-facing regulation input
   - D5 semantic breadth remains deferred

6. Add tests proving:
   - source preset id/schema/hash are preserved
   - family id and optional family variant id survive compilation
   - override lineage, canonical semantic reference, and stable semantic identifiers survive compilation
   - execution snapshot hashes are stable for unchanged inputs
   - run compiler no longer emits raw regulation maps in compiled requests
  </action>
  <verify>
Run `npm run typecheck --workspace apps/local-api --workspace packages/domain`.
Run `npm run test --workspace apps/local-api -- regulationCompiler`.
Run `npm run test --workspace apps/local-api -- runCompiler`.
Manually confirm a compiled request includes ExecutionRegulationSnapshot metadata and no raw preset `values` payload.
  </verify>
  <done>
Execution regulation data is compiled before backend submission, family identity is preserved, and raw preset interpretation is isolated to one compiler service.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread the execution snapshot through sim-core and remove hardcoded 2026 runtime assumptions</name>
  <files>
    packages/sim-core/src/stintModel/types.ts
    packages/sim-core/src/stintModel/electricalModel.ts
    packages/sim-core/src/stintModel/stintRunner.ts
    packages/sim-core/src/raceModel/types.ts
    packages/sim-core/src/raceModel/raceRunner.ts
    packages/sim-core/src/__tests__/electricalModel.test.ts
    packages/sim-core/src/__tests__/raceModel.test.ts
  </files>
  <action>
Remove the runtime leaks that keep the current boundary effectively single-family.

1. Thread the execution regulation capability data into the runtime config types used by:
   - stint execution
   - race execution
   - any backend adapter path that currently builds aero/electrical inputs ad hoc

2. Update `packages/sim-core/src/stintModel/electricalModel.ts` so:
   - initialization and per-lap limits come from compiled electrical capability data
   - hardcoded `REGULATION_2026` authority disappears from the runtime path
   - defaults remain only where they are compatibility or fallback scaffolding, not the authoritative compute input

3. Update aero handling so:
   - straight-mode / active-aero execution inputs arrive from the compiled snapshot and compiler output
   - `runService.ts` or its split descendants stop re-casting raw preset `aero` subtrees as the runtime contract

4. Update race execution so:
   - refueling capability is explicit
   - unsupported refueling-enabled snapshots fail honestly or are rejected before execution
   - there is no remaining implicit '2026 means no refueling' assumption in the runtime path

5. Add or update tests proving:
   - electrical limits are parameterized by compiled capability inputs
   - a refueling-enabled snapshot is handled explicitly rather than silently ignored
   - race and stint execution still work for the current 2026 baseline using the new snapshot-threaded path
  </action>
  <verify>
Run `npm run typecheck --workspace packages/sim-core`.
Run `npm run test --workspace packages/sim-core -- electricalModel`.
Run `npm run test --workspace packages/sim-core -- raceModel`.
Search the touched runtime files to confirm the hardcoded 2026 electrical constants are no longer the authoritative execution path.
  </verify>
  <done>
The backend boundary now carries an execution snapshot that the runtime actually honors, and the code no longer quietly depends on raw preset maps or implicit 2026-only assumptions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Carry compiled semantic lineage through backend, persistence, and compatibility surfaces</name>
  <files>
    apps/local-api/src/services/localSimulationBackend.ts
    apps/local-api/src/services/runExecutionService.ts
    apps/local-api/src/services/runPersistenceService.ts
    apps/local-api/src/persistence/artifactStore.ts
    apps/local-api/src/workers/localSimulationWorker.ts
    apps/local-api/src/__tests__/localSimulationBackend.test.ts
    apps/local-api/src/__tests__/runCompatibilityRoutes.test.ts
  </files>
  <action>
Make the D5 handshake survive every layer that actually carries execution results.

1. Update the worker/backend path so compiled requests keep their execution-regulation identity intact across dispatch, execution, artifact emission, and terminal job snapshots.

2. Update persistence and compatibility materialization so stored runs and artifact envelopes record:
   - raw source preset identity/hash
   - family id and optional family variant id
   - override lineage
   - canonical semantic reference
   - stable semantic identifiers required downstream
   - execution snapshot hash and compiler version

3. Ensure compatibility read models keep exposing honest raw-plus-compiled regulation lineage even when current web panels still consume legacy-shaped run objects.

4. Add or update tests proving:
   - a compiled request submitted through the backend keeps the same semantic reference and override lineage through terminal persistence
   - compatibility `/api/runs` responses still work while exposing the stored raw-plus-compiled provenance the phase now promises
   - terminal job snapshots and replayed events expose the same execution-regulation identity that the stored run and artifact envelope carry
   - artifact envelopes fetched after execution carry the same execution-regulation identity that entered the backend
  </action>
  <verify>
Run `npm run typecheck --workspace packages/domain`.
Run `npm run typecheck --workspace apps/local-api`.
Run `npm run test --workspace apps/local-api -- localSimulationBackend`.
Run `npm run test --workspace apps/local-api -- runCompatibilityRoutes`.
Run `npm run test --workspace apps/local-api -- jobRoutes`.
Run `npm run test --workspace packages/domain`.
Manually inspect one persisted run plus one artifact envelope from the new path to confirm raw-plus-compiled regulation lineage survives worker execution and persistence.
  </verify>
  <done>
The D5 compile-to-execution handshake survives not just compilation and sim-core, but also the worker, persistence, artifact, and compatibility layers that later phases will actually consume.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck --workspace packages/domain`
- [ ] `npm run typecheck --workspace apps/local-api`
- [ ] `npm run typecheck --workspace packages/sim-core`
- [ ] `npm run test --workspace packages/domain`
- [ ] `npm run test --workspace apps/local-api -- regulationCompiler`
- [ ] `npm run test --workspace apps/local-api -- runCompiler`
- [ ] `npm run test --workspace apps/local-api -- localSimulationBackend`
- [ ] `npm run test --workspace apps/local-api -- runCompatibilityRoutes`
- [ ] `npm run test --workspace apps/local-api -- jobRoutes`
- [ ] `npm run test --workspace packages/sim-core -- electricalModel`
- [ ] `npm run test --workspace packages/sim-core -- raceModel`
- [ ] Persisted runs and artifact envelopes preserve raw-plus-compiled regulation lineage, including override lineage and canonical semantic reference
- [ ] Terminal job snapshots and replayed events preserve the same execution-regulation identity promised by the typed schemas
- [ ] No runtime execution path still depends on raw regulation preset casts or hardcoded 2026 electrical constants
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Compiled requests carry ExecutionRegulationSnapshot as the runtime-facing regulation input
- sim-core electrical and race execution consume explicit capability data
- run and artifact provenance preserve raw-plus-compiled regulation lineage for later phases
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-backend-execution-job-artifacts-and-regulation-execution-flow/03.2-05-SUMMARY.md`
</output>
