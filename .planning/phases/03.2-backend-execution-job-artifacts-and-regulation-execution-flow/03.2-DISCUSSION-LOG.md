# Phase 3.2: Backend Execution, Job Artifacts, and Regulation Execution Flow — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-15 (refresh pass on the 2026-04-16 original)
**Phase:** 03.2-backend-execution-job-artifacts-and-regulation-execution-flow
**Mode:** `--auto` (single-pass autonomous; no AskUserQuestion calls; recommended option selected for each gray area; log preserves the alternatives)
**Areas discussed:** Typed-claim notation post-pruning, Strategic-audit intervention integration, INT-33 deliberation status, Cross-model verification path, Runtime-platform contradiction in config.json, Typed-claim validation enforcement plan
**Trigger:** Existing CONTEXT.md (`has_context: true`) + existing 5 plans (`plan_count: 5`); under `--auto`, init auto-selected "Update it" + "Continue and replan after". Refresh prompted by the 2026-05-01 strategic audit and the 2026-05-15 GSD ecosystem pivot.

---

## Typed-claim notation post-pruning

| Option | Description | Selected |
|--------|-------------|----------|
| Leave as-is with meta note | Keep all `[evidenced:cited]` / `[governing:cited]` / `[assumed:reasoned]` annotations; add a refresh note explaining `gsdr-context-checker` is pruned and manual review replaces validation. | ✓ |
| Strip all typed annotations | Convert to plain prose; eliminate machine-readable claim markers entirely. | |
| Convert to a custom local schema | Author a new repo-local validator under `.claude/agents/`. | |

**Selection rationale:** Annotations communicate epistemic intent (cited vs. reasoned vs. evidence-anchored) and remain valid markdown without the validator. Stripping would be churn for cosmetic reasons; authoring a replacement validator is fresh tooling debt that the just-completed pivot was specifically rejecting. Manual reviewer discipline scales for a single-developer research project.

---

## Strategic-audit intervention integration

| Option | Description | Selected |
|--------|-------------|----------|
| Reference INTERVENTIONS.md from canonical_refs + list 3.2-scoped bundle in `<specifics>` | Surface the agenda (INT-21/22/23/27/28/29) with suggested plan placement; let plan-phase make the final placement call. | ✓ |
| Inline full intervention text into `<working_model>` | Embed the audit findings as governing claims in the working model. | |
| Defer entirely to plan-phase | Mention the audit happened but do not list specific INTs in CONTEXT.md. | |

**Selection rationale:** CONTEXT.md captures decisions and signposts; placement is plan-phase's call. Listing the audit's suggested plan homes ("INT-21 → plan 03.2-03") with explicit "final placement is plan-phase's call" language preserves agency without dropping the agenda.

---

## INT-33 async-backend deliberation status

| Option | Description | Selected |
|--------|-------------|----------|
| Cite existing D1 deliberation record; INT-33 satisfied | The accepted record at `01-backend-boundary-architecture.md` already concretely decides compiled-request / async-job / artifact / execution-snapshot semantics. | ✓ |
| Run a fresh `/gsdr:deliberate` to ratify post-audit | The `gsdr:deliberate` command was retained in the pivot; re-deliberation could surface post-audit considerations. | |
| Defer | Skip the question for now. | |

**Selection rationale:** The D1 deliberation is already concrete, cited in `<canonical_refs>`, and consumed by the existing CONTEXT.md. A fresh deliberation would re-run a closed loop. The strategic audit's INT-33 framing was useful as a checkpoint when GSDR command coverage was uneven, but the substantive decision is on record. Plan-phase proceeds.

---

## Cross-model verification path

| Option | Description | Selected |
|--------|-------------|----------|
| `codex exec review` per CLAUDE.md `Quality Gates` | Documented baseline; consistent with the strategic-audit re-aim in INTERVENTIONS.md (INT-34 marked "re-aim required, substitute: manual codex exec review workflow"). | ✓ |
| Hand-authored prompt panel | Write a bespoke per-phase audit prompt panel. | |
| Skip cross-model audit | Defer the audit gate until tooling matures. | |

**Selection rationale:** Already the documented baseline in CLAUDE.md (`Cross-model audit — run codex exec review for independent Codex assessment`). The memory entry `reference_codex_audits.md` documents the flags and pitfalls. Skipping would violate the repo's audit-discipline feedback memory.

---

## Runtime-platform contradiction in config.json

| Option | Description | Selected |
|--------|-------------|----------|
| CONTEXT.md and plans stay runtime/agent-agnostic; flag config.json orphan but do not migrate inside 3.2 | Treat the `runtime.platform: codex` + `default_model: gpt-5.4` + `runtime.agent_overrides` as informational legacy; product code/schemas/artifacts must remain runtime-agnostic. | ✓ |
| Migrate config.json to Claude semantics inside 3.2 | Update the runtime block as part of the 3.2 work. | |
| Leave inconsistency unaddressed | No CONTEXT.md mention. | |

**Selection rationale:** Runtime/agent choice is tooling, not product. The execution-boundary contracts (`CompiledSimulationRequest`, `SimulationBackend`, `JobLifecycle`, `ArtifactEnvelope`, `ExecutionRegulationSnapshot`) are agent-independent by design. Migrating the config inside 3.2 conflates concerns and risks scope creep. STATE.md already records the orphan status; future config rewrites should drop the block.

---

## Typed-claim validation enforcement plan

| Option | Description | Selected |
|--------|-------------|----------|
| Manual reviewer pass per phase; no auto-validation | Single-developer research project; manual discipline replaces the pruned `gsdr-context-checker`. | ✓ |
| Author a new validator under `.claude/agents/` | Build a replacement structural validator. | |
| Drop typed-claim notation entirely | Remove the convention; flatten to prose. | |

**Selection rationale:** Solo research project; manual review is sufficient. A new validator is fresh tooling debt against the just-completed pivot intent (move from custom infrastructure to mainline GSD-1). Dropping notation entirely would also discard the epistemic-intent benefit captured in gray-area 1.

---

## Claude's Discretion

- Final intervention placement (which of the 5 plans absorbs INT-21/22/23/27/28/29, vs. defer with rationale) is plan-phase's decision — CONTEXT.md only suggests homes.
- The mechanical content of the working model (D1 contracts, regulation snapshot fields, artifact envelope shape) was carried forward unchanged; the refresh added only the post-2026-04-16 deltas (audit, pivot, tooling posture).
- No re-evaluation of the 6 open questions in `<questions>`. They remain plan-phase research targets as originally framed.

## Deferred Ideas

None — the refresh stayed within the existing phase scope. Cross-cutting interventions outside 3.2 (CI quality-gate INT-09, ESLint browser-safe rules INT-13, etc.) remain on the strategic-audit ledger and are explicitly NOT folded here.

## Auto-mode log (literal)

```
[--auto] init.phase-op resolved: phase_found=true, has_context=true, has_plans=true, plan_count=5
[--auto] Context exists — updating with auto-selected decisions
[--auto] Plans exist (5) — continuing with context capture, will replan after via auto_advance
[--auto] todo.match-phase 03.2: 0 explicit matches in todo_count=1 — no folding required
[--auto] codebase maps directory not present — using inline canonical_refs from prior CONTEXT.md as ground truth
[--auto] No SPEC.md, no .continue-here.md, no DISCUSS-CHECKPOINT — single-pass refresh path
[--auto] Selected all gray areas: typed-claim-notation, audit-integration, INT-33-status, verification-path, config-runtime-contradiction, typed-claim-enforcement
[--auto] Each gray area resolved with recommended option (logged inline in CONTEXT.md `<working_model>` and `<specifics>` and in this log's per-area tables)
[--auto] CONTEXT.md update: 5 targeted Edits (header refresh note, canonical_refs append, working_model append, specifics append, footer refresh marker)
[--auto] Single pass cap respected — no re-read for gap detection
```
