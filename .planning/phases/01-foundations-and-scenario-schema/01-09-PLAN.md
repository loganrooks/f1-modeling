---
phase: 01-foundations-and-scenario-schema
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/__tests__/documents.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "npm run typecheck passes from the repository root with zero errors"
    - "npm run build passes from the repository root with zero errors"
    - "npm test still passes after the type-safety fix"
  artifacts:
    - path: "packages/domain/src/__tests__/documents.test.ts"
      provides: "Type-safe preset fixture lookups under noUncheckedIndexedAccess"
      contains: "catalog.regulation[0]"
  key_links:
    - from: "packages/domain/src/__tests__/documents.test.ts"
      to: "packages/domain/src/runs/createRunRecord.ts"
      via: "resolvedPresets argument to createRunRecord"
      pattern: "createRunRecord"
---

<objective>
Fix unchecked array indexing in domain contract tests so the shared repository validation surface is clean.

Purpose: The Phase 1 verification found that `npm run typecheck` and `npm run build` both fail from the repo root because `packages/domain/src/__tests__/documents.test.ts` passes `catalog.regulation[0]`, `catalog.session[0]`, and `catalog.weather[0]` into `createRunRecord(...)` without narrowing the possibly-undefined result of indexed access under `noUncheckedIndexedAccess`. This is the sole blocker preventing Truth 5 ("stable shared engineering baseline") from passing.
Output: A clean `documents.test.ts` that satisfies both the runtime test suite and the TypeScript compiler.
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/domain/src/__tests__/documents.test.ts
@packages/domain/src/runs/createRunRecord.ts
@packages/domain/tsconfig.json
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow preset fixture lookups in domain contract tests</name>
  <files>packages/domain/src/__tests__/documents.test.ts</files>
  <action>
Fix the six TS2719 errors caused by unchecked array indexing under `noUncheckedIndexedAccess` (enabled in `tsconfig.base.json`).

In the test at line ~214 ("creates an immutable run record with preset references and snapshots"), the `resolvedPresets` object passes `catalog.regulation[0]`, `catalog.session[0]`, and `catalog.weather[0]` directly. Under `noUncheckedIndexedAccess`, these expressions have type `T | undefined`, but `ResolvedPresetDocuments` requires non-optional `RegulationPresetDocument`, `SessionPresetDocument`, and `WeatherPresetDocument`.

Fix approach: Add a shared helper at the top of the test file (after imports, near the existing `createPresetSnapshot` and `createValidRunRecord` helpers) that extracts and asserts the first element of each catalog array, throwing early if a preset is unexpectedly missing. For example:

```typescript
function firstPreset<T>(presets: T[], label: string): T {
  const first = presets[0];
  if (first === undefined) {
    throw new Error(`Expected at least one ${label} preset in catalog`);
  }
  return first;
}
```

Then in the test at lines 222-224 (positive path), replace:
```typescript
regulation: catalog.regulation[0],
session: catalog.session[0],
weather: catalog.weather[0],
```
with:
```typescript
regulation: firstPreset(catalog.regulation, "regulation"),
session: firstPreset(catalog.session, "session"),
weather: firstPreset(catalog.weather, "weather"),
```

And in the test at lines 275-277 (negative path, the "rejects run construction when resolved presets do not match" test), apply the same pattern. Note that this test intentionally uses `catalog.session[1]` for the mismatch case, so use `catalog.session[1]` with the same narrowing pattern — extract it and assert it exists before passing it.

Do NOT change the `packages/domain/tsconfig.json` to exclude tests — the current include pattern (`src/**/*`) is intentional and correct. The fix must be in the test code itself.

Do NOT weaken the TypeScript strictness settings. The `noUncheckedIndexedAccess` flag is a project-wide safety net.
  </action>
  <verify>
Run all three validation commands from the repository root:
1. `npm run typecheck` — must exit 0 with no errors
2. `npm run build` — must exit 0 with no errors
3. `npm test` — must exit 0 with all existing tests still passing
  </verify>
  <done>
All three root commands (`typecheck`, `build`, `test`) pass cleanly. The domain contract tests remain functionally identical — same assertions, same coverage, same negative-path validation — but with type-safe preset lookups that satisfy `noUncheckedIndexedAccess`.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` exits 0 (previously failed with 6 TS2719 errors in `@f1-modeling/domain`)
2. `npm run build` exits 0 (previously failed for the same reason)
3. `npm test` exits 0 with all tests passing (no behavioral regression)
4. No changes to `tsconfig.base.json` or `packages/domain/tsconfig.json` strictness settings
</verification>

<success_criteria>
The shared repository validation surface is clean: `npm run typecheck`, `npm run build`, and `npm test` all pass from the repo root. Truth 5 from Phase 1 verification ("The shared repository validation surface is stable enough for later phases to build on directly") is now satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-09-SUMMARY.md`
</output>
