---
phase: 01-foundations-and-scenario-schema
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/vite.config.ts
  - apps/web/index.html
  - apps/web/src/vite-env.d.ts
  - apps/web/src/main.tsx
  - apps/web/src/app/App.tsx
  - apps/web/src/styles/global.css
  - apps/local-api/package.json
  - apps/local-api/tsconfig.json
  - apps/local-api/src/app.ts
  - apps/local-api/src/server.ts
  - apps/local-api/src/routes/health.ts
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "The repo has separate `apps/web` and `apps/local-api` workspace shells instead of a single flat app."
    - "The browser shell reads as an engineering workbench placeholder, not a marketing page or KPI dashboard."
    - "The local API exposes `/api/health`, and the web shell can surface that local stack status."
    - "This plan scaffolds the local workbench shell only; it does not add shared domain contracts, persistence routes, or simulation logic."
  artifacts:
    - path: "apps/web/src/app/App.tsx"
      provides: "initial browser workbench shell with API status stub"
      min_lines: 40
    - path: "apps/web/vite.config.ts"
      provides: "Vite configuration that proxies `/api` to the local API"
      contains: "8787"
    - path: "apps/local-api/src/server.ts"
      provides: "Fastify server entrypoint"
      contains: "listen"
    - path: "apps/local-api/src/routes/health.ts"
      provides: "health endpoint for local stack verification"
      contains: "/api/health"
  key_links:
    - from: "apps/web/vite.config.ts"
      to: "apps/local-api/src/server.ts"
      via: "proxy target points the browser shell at the local API port"
      pattern: "8787"
    - from: "apps/local-api/src/app.ts"
      to: "apps/local-api/src/routes/health.ts"
      via: "Fastify route registration"
      pattern: "health"
    - from: "apps/web/src/app/App.tsx"
      to: "apps/local-api/src/routes/health.ts"
      via: "UI status check for the local-first stack"
      pattern: "health"
---

<objective>
Scaffold the two runnable application shells for Phase 1: the browser workbench in `apps/web` and the local Fastify API in `apps/local-api`.

Purpose: Phase 1 needs a local browser workspace plus a local API process, but this plan must stop at shell-level scaffolding and health wiring.

Output: A Vite React app shell, a Fastify API shell, and a root dev workflow that can start both locally.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@.planning/phases/01-foundations-and-scenario-schema/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the browser workbench shell in `apps/web`</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/web/vite.config.ts, apps/web/index.html, apps/web/src/vite-env.d.ts, apps/web/src/main.tsx, apps/web/src/app/App.tsx, apps/web/src/styles/global.css</files>
  <action>
    1. Scaffold `apps/web` as a React + Vite + TypeScript workspace with local scripts for `dev`, `build`, `test`, and `typecheck`.

    2. Build the initial `App.tsx` as a three-zone engineering workbench stub: editable space on the left, results/workspace area in the middle, and assumptions/system state on the right. Keep copy honest about Phase 1 being a foundation.

    3. Configure `vite.config.ts` to proxy `/api` requests to `127.0.0.1:8787`, bind the browser dev server to `127.0.0.1:5173`, and set `strictPort: true` so later smoke tests can rely on a deterministic URL. Do not introduce SSR, hosted deployment, or any full-stack framework concerns here.

    4. Add intentional baseline styling in `global.css` using CSS variables. The direction should feel technical and inspectable, not generic dashboard boilerplate and not faux race telemetry.

    5. Show a visible local API status indicator in the shell, but do not add schema-driven editing, persistence, or comparison components yet.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/web
  </verify>
  <done>`apps/web` builds as a local workbench shell with a Vite proxy and an explicit API-status surface.</done>
</task>

<task type="auto">
  <name>Task 2: Create the local Fastify API shell in `apps/local-api`</name>
  <files>apps/local-api/package.json, apps/local-api/tsconfig.json, apps/local-api/src/app.ts, apps/local-api/src/server.ts, apps/local-api/src/routes/health.ts</files>
  <action>
    1. Scaffold `apps/local-api` as a Fastify + TypeScript workspace with local scripts for `dev`, `build`, `test`, and `typecheck`.

    2. Separate app construction from process startup:
       - `src/app.ts` creates/registers the Fastify instance
       - `src/server.ts` owns process startup and port binding

    3. Add `GET /api/health` returning small JSON that confirms the local API is alive. Bind the local API explicitly to `127.0.0.1:8787` so later smoke tests can poll a deterministic endpoint. This endpoint exists only for stack verification at this stage; do not add presets, scenarios, or runs yet.

    4. Ensure the root `npm run dev` flow from Plan 01 can start both the browser and API shells together.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && (npm run dev >/tmp/f1-phase1-shells.log 2>&1 & DEV_PID=$!; trap 'kill $DEV_PID 2>/dev/null || true' EXIT; READY=0; for i in $(seq 1 40); do if curl -sf http://127.0.0.1:8787/api/health >/dev/null && curl -sf http://127.0.0.1:5173 >/dev/null; then READY=1; break; fi; sleep 1; done; test "$READY" = 1; curl -sf http://127.0.0.1:8787/api/health >/dev/null; curl -sf http://127.0.0.1:5173 >/dev/null; kill $DEV_PID; trap - EXIT)
  </verify>
  <done>The root dev workflow can start both app shells, `/api/health` responds, and the browser shell loads locally.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace @f1-modeling/web` passes.
- Starting `npm run dev` and polling the pinned `127.0.0.1:8787/api/health` and `127.0.0.1:5173` endpoints reaches both shells deterministically.
</verification>

<success_criteria>
- `apps/web` and `apps/local-api` exist as separate, runnable shells.
- The local stack boots without requiring shared domain or persistence code yet.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-02-SUMMARY.md`
</output>
