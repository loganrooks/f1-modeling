---
phase: 01-foundations-and-scenario-schema
plan: 08
type: execute
wave: 6
depends_on: ["01-02", "01-05", "01-06", "01-07"]
files_modified:
  - package-lock.json
  - apps/web/package.json
  - apps/web/src/app/App.tsx
  - apps/web/src/app/useWorkspace.ts
  - apps/web/src/features/workspace/api.ts
  - apps/web/src/features/scenario/ScenarioEditor.tsx
  - apps/web/src/features/presets/PresetSelectors.tsx
  - apps/web/src/features/runs/RunHistoryPanel.tsx
  - apps/web/src/features/runs/RunSummaryPanel.tsx
  - apps/web/src/app/App.test.tsx
  - apps/web/src/styles/global.css
autonomous: true
requirements: [PLAT-01, PLAT-03, VAL-02, VISU-01]

must_haves:
  truths:
    - "The user can open the local browser workspace, edit a Phase 1 scenario, and save or reload it through the local API."
    - "The user can create a placeholder Phase 1 run and inspect recent run history in the workspace."
    - "The UI supports early side-by-side inspection by reusing the shared comparison and assumption primitives from the visuals package."
    - "The workspace clearly labels assumptions, provenance, and placeholder outputs without implying lap, telemetry, controller, or racing-line fidelity."
  artifacts:
    - path: "apps/web/src/app/useWorkspace.ts"
      provides: "client-side workflow hook for presets, scenarios, and run history"
      exports: ["useWorkspace"]
    - path: "apps/web/src/features/scenario/ScenarioEditor.tsx"
      provides: "editable Phase 1 scenario form"
      exports: ["ScenarioEditor"]
    - path: "apps/web/src/features/runs/RunHistoryPanel.tsx"
      provides: "workspace run-history list for saved Phase 1 runs"
      exports: ["RunHistoryPanel"]
    - path: "apps/web/src/app/App.tsx"
      provides: "first integrated Phase 1 workspace"
      min_lines: 100
    - path: "apps/web/src/app/App.test.tsx"
      provides: "workspace-level web tests"
      min_lines: 90
  key_links:
    - from: "apps/web/src/features/workspace/api.ts"
      to: "apps/local-api/src/routes/scenarios.ts"
      via: "browser workspace saves and reloads scenarios through the local API"
      pattern: "/api/scenarios"
    - from: "apps/web/src/features/workspace/api.ts"
      to: "apps/local-api/src/routes/runs.ts"
      via: "browser workspace creates and lists Phase 1 runs through the local API"
      pattern: "/api/runs"
    - from: "apps/web/src/app/App.tsx"
      to: "packages/visuals/src/index.ts"
      via: "workspace composition uses the shared visuals package instead of page-local primitives"
      pattern: "WorkbenchShell"
---

<objective>
Assemble the first integrated Phase 1 workspace on top of the shared contracts, API, and visual primitives.

Purpose: This plan closes the Phase 1 loop by delivering a usable local-first browser workspace without crossing the phase boundary into real simulator fidelity or later subsystem work.

Output: A browser workspace that edits scenarios, saves/loads them locally, creates placeholder runs, and renders shared comparison/provenance surfaces.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@.planning/phases/01-foundations-and-scenario-schema/01-02-SUMMARY.md
@.planning/phases/01-foundations-and-scenario-schema/01-05-SUMMARY.md
@.planning/phases/01-foundations-and-scenario-schema/01-06-SUMMARY.md
@.planning/phases/01-foundations-and-scenario-schema/01-07-SUMMARY.md

<interfaces>
From `@f1-modeling/domain`:
- `createDefaultScenario()`
- `scenarioDocumentSchema`
- preset document types and ids

Browser rule:
- the web app uses the browser-safe package root only for schemas, types, and factories
- the web app never imports the node-only preset disk loader

From the local API:
- `GET /api/presets`
- `GET /api/scenarios`
- `GET /api/scenarios/:scenarioId`
- `POST /api/scenarios`
- `GET /api/runs`
- `POST /api/runs`

From `@f1-modeling/visuals`:
- `WorkbenchShell`
- `RunComparisonCard`
- `MetricTracePanel`
- `AssumptionPanel`
- `TrackContextPlaceholder`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the browser-side workspace data layer and editing panels</name>
  <files>package-lock.json, apps/web/package.json, apps/web/src/app/useWorkspace.ts, apps/web/src/features/workspace/api.ts, apps/web/src/features/scenario/ScenarioEditor.tsx, apps/web/src/features/presets/PresetSelectors.tsx, apps/web/src/features/runs/RunHistoryPanel.tsx, apps/web/src/features/runs/RunSummaryPanel.tsx</files>
  <action>
    1. Implement `features/workspace/api.ts` as the single browser-side integration layer for presets, scenarios, and runs. Keep all fetch logic centralized there, and load preset data only through HTTP calls to the local API.

    2. Update `apps/web/package.json` to declare the explicit workspace dependencies it imports in this phase, including `@f1-modeling/domain` and `@f1-modeling/visuals`.

    3. Implement `useWorkspace.ts` to orchestrate:
       - loading the preset catalog
       - creating the default scenario
       - saving the current scenario
       - loading saved scenarios
       - creating a placeholder run
       - loading run history

    4. Build `ScenarioEditor.tsx` around the shared domain contract. The editable Phase 1 surface must include:
       - scenario name
       - circuit
       - session preset
       - regulation preset
       - weather preset
       - driver profile placeholder fields
       - seed
       - notes / assumptions

    5. Build `PresetSelectors.tsx`, `RunHistoryPanel.tsx`, and `RunSummaryPanel.tsx` so the user can inspect and compare the saved scenario and placeholder-run metadata. Do not add unsupported lap-model or controller-specific UI.

    6. After the package manifest changes, rerun root `npm install` so workspace resolution is current before the web typecheck and tests run.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/web
  </verify>
  <done>The browser workspace can load presets, edit a Phase 1 scenario, save/reload it through the API, and show placeholder run history through a single client-side data layer.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the integrated workbench layout, styling, and workspace tests</name>
  <files>apps/web/src/app/App.tsx, apps/web/src/app/App.test.tsx, apps/web/src/styles/global.css</files>
  <action>
    1. Compose `App.tsx` with the shared visuals package so the workspace follows the Phase 1 workbench layout:
       - left pane for scenario editing and preset selection
       - center pane for run summaries, comparison cards, and placeholder traces
       - right pane for assumptions/provenance and the honest track-context placeholder

    2. Update `global.css` so the page feels like a deliberate engineering workbench. Preserve the honest-visualization constraints from context: no fake racing lines, policy overlays, or telemetry-style claims.

    3. Keep the browser dev server pinned to `127.0.0.1:5173` with `strictPort: true` from Plan 01-02, and keep the API pinned to `127.0.0.1:8787` so the final smoke test can poll deterministic endpoints instead of guessing ports.

    4. Add `App.test.tsx` with React Testing Library coverage for the workspace load flow and at least one save/load or run-history interaction using mocked API responses.

    5. Keep placeholder outputs labeled as placeholder. The UI may visualize the deterministic Phase 1 harness output, but it must not imply validated lap physics or controller fidelity.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run test --workspace @f1-modeling/web -- --run && (npm run dev >/tmp/f1-phase1-workspace.log 2>&1 & DEV_PID=$!; trap 'kill $DEV_PID 2>/dev/null || true' EXIT; READY=0; for i in $(seq 1 40); do if curl -sf http://127.0.0.1:8787/api/presets >/dev/null && curl -sf http://127.0.0.1:8787/api/scenarios >/dev/null && curl -sf http://127.0.0.1:8787/api/runs >/dev/null && curl -sf http://127.0.0.1:5173 >/dev/null; then READY=1; break; fi; sleep 1; done; test "$READY" = 1; curl -sf http://127.0.0.1:8787/api/presets >/dev/null; curl -sf http://127.0.0.1:8787/api/scenarios >/dev/null; curl -sf http://127.0.0.1:8787/api/runs >/dev/null; curl -sf http://127.0.0.1:5173 >/dev/null; kill $DEV_PID; trap - EXIT)
  </verify>
  <done>The browser workspace is assembled, tested, and reachable locally with the Phase 1 API endpoints available under the running dev stack.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace @f1-modeling/web` passes before the final UI assembly test task adds `App.test.tsx`.
- `npm run test --workspace @f1-modeling/web -- --run` passes after `App.test.tsx` is in place.
- Starting the stack afterwards and polling the pinned `127.0.0.1:8787` and `127.0.0.1:5173` endpoints reaches the API and browser app cleanly.
</verification>

<success_criteria>
- The user can start the app locally and reach a usable Phase 1 workspace.
- The user can save/reload scenarios and create placeholder runs through the local API.
- The workspace uses shared visual primitives and remains honest about Phase 1 fidelity limits.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-08-SUMMARY.md`
</output>
