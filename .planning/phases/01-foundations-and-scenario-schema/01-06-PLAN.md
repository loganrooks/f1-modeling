---
phase: 01-foundations-and-scenario-schema
plan: 06
type: execute
wave: 5
depends_on: ["01-02", "01-03", "01-05"]
files_modified:
  - package-lock.json
  - apps/local-api/package.json
  - apps/local-api/src/app.ts
  - apps/local-api/src/persistence/fileStore.ts
  - apps/local-api/src/services/presetService.ts
  - apps/local-api/src/services/runService.ts
  - apps/local-api/src/routes/presets.ts
  - apps/local-api/src/routes/scenarios.ts
  - apps/local-api/src/routes/runs.ts
  - apps/local-api/src/__tests__/workspaceRoutes.test.ts
autonomous: true
requirements: [PLAT-03, VAL-02]

must_haves:
  truths:
    - "The local API can list presets, save and reload scenario documents, and create or list immutable run records."
    - "Scenario and run persistence writes pretty-printed JSON into `workspace-data/` so the canonical artifacts stay diffable and inspectable."
    - "Run creation flows through the shared domain helper and the Phase 1 placeholder harness rather than duplicating schema logic in the API."
    - "Runs are history records, not mutable workspace blobs."
  artifacts:
    - path: "apps/local-api/src/persistence/fileStore.ts"
      provides: "JSON file store for scenario and run documents"
      exports: ["readJsonFile", "writeJsonFile", "listJsonFiles"]
    - path: "apps/local-api/src/routes/scenarios.ts"
      provides: "validated scenario list/get/save routes"
      exports: ["registerScenarioRoutes"]
    - path: "apps/local-api/src/routes/runs.ts"
      provides: "append-only run routes"
      exports: ["registerRunRoutes"]
    - path: "apps/local-api/src/services/runService.ts"
      provides: "run creation service wired to sim-core and createRunRecord"
      exports: ["createPhase1Run", "listRuns"]
    - path: "apps/local-api/src/__tests__/workspaceRoutes.test.ts"
      provides: "Fastify route integration tests for presets, scenarios, and runs"
      min_lines: 120
  key_links:
    - from: "apps/local-api/src/services/runService.ts"
      to: "packages/domain/src/runs/createRunRecord.ts"
      via: "API run creation uses the shared append-only run helper"
      pattern: "createRunRecord"
    - from: "apps/local-api/src/services/runService.ts"
      to: "packages/sim-core/src/phase1Placeholder.ts"
      via: "API run creation uses the deterministic Phase 1 placeholder harness"
      pattern: "runPhase1PlaceholderScenario"
    - from: "apps/local-api/src/app.ts"
      to: "apps/local-api/src/routes/scenarios.ts"
      via: "Fastify registers the scenario routes under `/api`"
      pattern: "scenarios"
---

<objective>
Implement the file-backed local API for Phase 1 presets, scenarios, and run history.

Purpose: This plan turns the shared contracts into an inspectable persistence path on disk while keeping schema validation and run construction centralized in the shared packages.

Output: A tested local API that reads presets, saves scenarios, and appends run-history records in `workspace-data/`.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@.planning/phases/01-foundations-and-scenario-schema/01-02-SUMMARY.md
@.planning/phases/01-foundations-and-scenario-schema/01-03-SUMMARY.md
@.planning/phases/01-foundations-and-scenario-schema/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the file-backed store plus preset and scenario routes</name>
  <files>package-lock.json, apps/local-api/package.json, apps/local-api/src/app.ts, apps/local-api/src/persistence/fileStore.ts, apps/local-api/src/services/presetService.ts, apps/local-api/src/routes/presets.ts, apps/local-api/src/routes/scenarios.ts</files>
  <action>
    1. Implement `fileStore.ts` as the canonical Phase 1 JSON persistence utility. Writes must be pretty-printed, deterministic, and target `workspace-data/scenarios/` or `workspace-data/runs/` rather than hidden browser-managed storage.

    2. Update `apps/local-api/package.json` to declare the explicit workspace dependencies it imports in this phase, including `@f1-modeling/domain` and `@f1-modeling/sim-core`.

    3. Implement `presetService.ts` to load the shared preset catalog from `presets/` via the node-only preset-loader subpath defined in Plan 01-05, for example `@f1-modeling/domain/node/preset-catalog`. Do not duplicate preset schema definitions here, and do not import node-side disk loading from the browser-safe package root.

    4. Add the preset and scenario routes:
       - `GET /api/presets`
       - `GET /api/scenarios`
       - `GET /api/scenarios/:scenarioId`
       - `POST /api/scenarios`

    5. `POST /api/scenarios` may upsert by `scenarioId`, but every write must validate against `scenarioDocumentSchema` first.

    6. Register the new routes in `apps/local-api/src/app.ts` under the `/api` prefix.

    7. After the package manifest changes, rerun root `npm install` so workspace resolution is current before the local API typecheck and tests run.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/local-api
  </verify>
  <done>The local API can validate, persist, and reload scenarios while exposing the shared preset catalog from disk.</done>
</task>

<task type="auto">
  <name>Task 2: Add append-only run creation routes and API integration tests</name>
  <files>apps/local-api/src/services/runService.ts, apps/local-api/src/routes/runs.ts, apps/local-api/src/__tests__/workspaceRoutes.test.ts, apps/local-api/src/app.ts</files>
  <action>
    1. Implement `runService.ts` so run creation:
       - resolves the saved scenario
       - resolves the referenced preset documents
       - executes `runPhase1PlaceholderScenario`
       - builds an immutable run record through `createRunRecord`
       - writes that run to `workspace-data/runs/`

    2. Add run routes:
       - `GET /api/runs`
       - `POST /api/runs`

       Runs are append-only history records. Do not create update/delete endpoints in Phase 1.

    3. Add `workspaceRoutes.test.ts` using Fastify injection plus a temp workspace root. Cover preset listing, scenario save/load, run creation, run listing, and the requirement that persisted run JSON contains both ids and snapshots.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run test --workspace @f1-modeling/local-api -- --run
  </verify>
  <done>The local API can create and list append-only Phase 1 run records, and route-level integration tests prove the persistence path works.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace @f1-modeling/local-api` passes.
- `npm run test --workspace @f1-modeling/local-api -- --run` passes with route integration coverage.
</verification>

<success_criteria>
- Presets, scenarios, and runs flow through a local JSON persistence layer.
- Run creation reuses the shared placeholder harness and snapshot-aware domain helper.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-06-SUMMARY.md`
</output>
