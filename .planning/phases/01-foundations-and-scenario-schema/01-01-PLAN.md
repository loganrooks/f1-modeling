---
phase: 01-foundations-and-scenario-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - README.md
  - tsconfig.base.json
autonomous: true
requirements: [PLAT-01, DESN-02]

must_haves:
  truths:
    - "The repo root declares npm workspaces for `apps/*` and `packages/*` so later plans plug into a stable monorepo contract."
    - "Shared root scripts exist for `dev`, `build`, `test`, and `typecheck`, even though the application workspaces arrive in later plans."
    - "The root toolchain and docs describe a local-first engineering workbench foundation rather than a hosted app or a simulator release."
    - "Ignore rules preserve future `workspace-data/` JSON artifacts as human-inspectable local files instead of treating them as browser-only state."
  artifacts:
    - path: "package.json"
      provides: "workspace root, shared scripts, and Phase 1 toolchain dependencies"
      contains: "\"workspaces\""
    - path: "tsconfig.base.json"
      provides: "shared TypeScript compiler baseline for all future workspaces"
      contains: "\"compilerOptions\""
    - path: "README.md"
      provides: "updated Phase 1 bootstrap instructions and scope boundary"
      contains: "local-first"
    - path: ".gitignore"
      provides: "ignore rules for generated output and future workspace-data JSON"
      contains: "workspace-data"
  key_links:
    - from: "package.json"
      to: "tsconfig.base.json"
      via: "root typecheck/build scripts rely on the shared TS baseline"
      pattern: "tsconfig.base.json"
    - from: "README.md"
      to: "package.json"
      via: "bootstrap instructions reference the root npm scripts"
      pattern: "npm run"
---

<objective>
Create the root monorepo contract for Phase 1: npm workspaces, shared toolchain dependencies, root scripts, and repo-level documentation/ignore rules.

Purpose: Every later Phase 1 plan depends on a stable local-first workspace root, but this plan must stop before creating app shells or shared package seams.

Output: A root `package.json`, base TypeScript config, repo docs, and ignore rules that define how the rest of Phase 1 will plug into the repository.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the root workspace contract and shared toolchain</name>
  <files>package.json, package-lock.json, .gitignore, README.md, tsconfig.base.json</files>
  <action>
    1. Create the repo root `package.json` as a private npm-workspaces monorepo with `apps/*` and `packages/*`. This plan must define the shared dependency/tooling baseline recommended by research:
       - TypeScript
       - `tsx`
       - `vitest`
       - `concurrently`
       - `react`, `react-dom`, `vite`, `@vitejs/plugin-react`
       - `fastify`
       - `zod`
       - `@testing-library/react`, `@testing-library/jest-dom`, and `jsdom`

    2. Add root scripts for `dev`, `build`, `test`, and `typecheck`. It is acceptable that some scripts only become runnable after later plans create the referenced workspaces; the important contract here is that the root command surface is fixed now.

    3. Create `tsconfig.base.json` with shared compiler settings suitable for a TypeScript-first local web workbench and local API.

    4. Update `.gitignore` so `node_modules`, build output, and future generated workspace artifacts are ignored, while Phase 1 planning still leaves room for tracked `.gitkeep` directories and inspectable JSON documents.

    5. Update `README.md` to document the monorepo direction, the local-first browser-plus-local-API shape, and the explicit Phase 1 boundary: no lap model, telemetry ingestion, observer methods, MPC, or RL implementation in this phase.

    6. Run `npm install` so the root lockfile exists before downstream plans add workspaces.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm install && node -e "const pkg=require('./package.json'); const ws=Array.isArray(pkg.workspaces)?pkg.workspaces:pkg.workspaces?.packages; if (!ws || !ws.includes('apps/*') || !ws.includes('packages/*')) throw new Error('workspace globs missing'); for (const key of ['dev','build','test','typecheck']) if (!pkg.scripts?.[key]) throw new Error('missing script '+key);" && test -f tsconfig.base.json
  </verify>
  <done>The repo root defines the workspace contract, shared toolchain, and documented Phase 1 boundary, and `npm install` succeeds from the root.</done>
</task>

</tasks>

<verification>
- `npm install` completes from the repo root.
- `package.json` contains the workspace globs and the shared root scripts.
- `README.md` documents the local-first Phase 1 foundation without overreaching into later modeling work.
</verification>

<success_criteria>
- Root toolchain and monorepo structure are fixed before any app or package scaffolding begins.
- The repo documentation and ignore rules match the inspectable local-artifact direction from Phase 1 research.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-01-SUMMARY.md`
</output>
