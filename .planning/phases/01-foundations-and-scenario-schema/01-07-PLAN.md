---
phase: 01-foundations-and-scenario-schema
plan: 07
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/visuals/src/workspace/WorkbenchShell.tsx
  - packages/visuals/src/cards/RunComparisonCard.tsx
  - packages/visuals/src/traces/MetricTracePanel.tsx
  - packages/visuals/src/inspectors/AssumptionPanel.tsx
  - packages/visuals/src/workspace/TrackContextPlaceholder.tsx
  - packages/visuals/src/index.ts
autonomous: true
requirements: [VISU-01]

must_haves:
  truths:
    - "Phase 1 establishes reusable visual primitives in the shared visuals package instead of hard-coding everything in the app shell."
    - "The visual language supports workbench layout, comparison, trace display, and assumptions/provenance inspection early."
    - "Track context remains an explicit placeholder surface and does not imply trajectory or racing-line fidelity that the model does not support."
    - "The shared visuals package exports stable building blocks for later phases to reuse."
  artifacts:
    - path: "packages/visuals/src/workspace/WorkbenchShell.tsx"
      provides: "shared workbench layout primitive"
      exports: ["WorkbenchShell"]
    - path: "packages/visuals/src/cards/RunComparisonCard.tsx"
      provides: "shared comparison card for run metadata"
      exports: ["RunComparisonCard"]
    - path: "packages/visuals/src/inspectors/AssumptionPanel.tsx"
      provides: "shared assumptions and provenance surface"
      exports: ["AssumptionPanel"]
    - path: "packages/visuals/src/workspace/TrackContextPlaceholder.tsx"
      provides: "honest placeholder for future track/geometry context"
      exports: ["TrackContextPlaceholder"]
  key_links:
    - from: "packages/visuals/src/index.ts"
      to: "packages/visuals/src/workspace/WorkbenchShell.tsx"
      via: "visuals package entrypoint exports the shared layout primitive"
      pattern: "WorkbenchShell"
    - from: "packages/visuals/src/index.ts"
      to: "packages/visuals/src/inspectors/AssumptionPanel.tsx"
      via: "visuals package entrypoint exports provenance-aware UI surfaces"
      pattern: "AssumptionPanel"
    - from: "packages/visuals/src/index.ts"
      to: "packages/visuals/src/workspace/TrackContextPlaceholder.tsx"
      via: "visuals package entrypoint exports the honest placeholder surface"
      pattern: "TrackContextPlaceholder"
---

<objective>
Create the reusable visual primitives that establish the Phase 1 learning surface.

Purpose: Visualization is a first-class part of this product, but Phase 1 must build honest reusable primitives instead of unsupported fidelity cues or one-off page markup.

Output: A shared visuals package with workbench, comparison, trace, assumptions, and track-context placeholder primitives.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@.planning/phases/01-foundations-and-scenario-schema/01-02-SUMMARY.md
@.planning/phases/01-foundations-and-scenario-schema/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the shared workbench and comparison primitives</name>
  <files>packages/visuals/src/workspace/WorkbenchShell.tsx, packages/visuals/src/cards/RunComparisonCard.tsx, packages/visuals/src/traces/MetricTracePanel.tsx, packages/visuals/src/index.ts</files>
  <action>
    1. Implement `WorkbenchShell` as a reusable three-zone layout primitive that later phases can keep using as the workbench grows richer.

    2. Implement `RunComparisonCard` and `MetricTracePanel` as shared, props-driven primitives. They must stay presentation-focused and avoid app-local fetching or persistence logic.

    3. Re-export the new primitives from `packages/visuals/src/index.ts` so downstream app code consumes the package entrypoint instead of deep private imports.

    4. Keep component APIs generic enough for later model outputs, but do not design them around fake trajectory, controller, or telemetry semantics that do not exist yet.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/visuals
  </verify>
  <done>The visuals package exports reusable layout, comparison, and trace primitives with presentation-only APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Add assumptions/provenance and honest track-context placeholder primitives</name>
  <files>packages/visuals/src/inspectors/AssumptionPanel.tsx, packages/visuals/src/workspace/TrackContextPlaceholder.tsx, packages/visuals/src/index.ts</files>
  <action>
    1. Implement `AssumptionPanel` so later phases have a shared surface for fact vs inference vs placeholder explanations, provenance notes, and model/version context.

    2. Implement `TrackContextPlaceholder` as an intentionally modest placeholder for future geometry or overlay surfaces. Its copy and visual treatment must make the lack of trajectory/racing-line fidelity obvious.

    3. Re-export both primitives from the visuals entrypoint and keep them reusable across later phases.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/visuals
  </verify>
  <done>The shared visuals package includes reusable provenance and placeholder-context surfaces without implying unsupported model fidelity.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace @f1-modeling/visuals` passes.
- The visuals package entrypoint exports all Phase 1 primitives.
</verification>

<success_criteria>
- Phase 1 has reusable visual building blocks before the app assembles the first workspace.
- Honest-visualization constraints from `01-CONTEXT.md` are encoded in the shared primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-07-SUMMARY.md`
</output>
