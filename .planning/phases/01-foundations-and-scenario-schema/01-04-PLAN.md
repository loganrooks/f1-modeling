---
phase: 01-foundations-and-scenario-schema
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - packages/domain/src/common/schemaVersion.ts
  - packages/domain/src/common/provenance.ts
  - packages/domain/src/scenario/schema.ts
  - packages/domain/src/scenario/defaultScenario.ts
  - packages/domain/src/runs/schema.ts
  - packages/domain/src/index.ts
  - packages/domain/src/__tests__/documents.test.ts
autonomous: true
requirements: [PLAT-03, VAL-02, DESN-02]

must_haves:
  truths:
    - "Scenario documents validate as their own versioned contract instead of being embedded inside one opaque workspace blob."
    - "Shared provenance metadata distinguishes documented fact, engineering inference, and placeholder assumptions."
    - "The Phase 1 domain package can construct a deterministic default scenario without UI-local ad hoc objects."
    - "Run-record shape exists with reproducibility fields, but append-only construction is still deferred to the next plan."
  artifacts:
    - path: "packages/domain/src/scenario/schema.ts"
      provides: "versioned scenario document schema for Phase 1"
      exports: ["scenarioDocumentSchema", "ScenarioDocument"]
    - path: "packages/domain/src/scenario/defaultScenario.ts"
      provides: "default scenario factory"
      exports: ["createDefaultScenario"]
    - path: "packages/domain/src/runs/schema.ts"
      provides: "base run-record schema with reproducibility fields"
      exports: ["runRecordSchema", "RunRecord"]
    - path: "packages/domain/src/common/provenance.ts"
      provides: "shared provenance and source-type contract"
      exports: ["sourceTypeSchema", "provenanceSchema"]
    - path: "packages/domain/src/__tests__/documents.test.ts"
      provides: "core domain schema tests"
      min_lines: 90
  key_links:
    - from: "packages/domain/src/scenario/defaultScenario.ts"
      to: "packages/domain/src/scenario/schema.ts"
      via: "default factory returns a validated ScenarioDocument shape"
      pattern: "scenarioDocumentSchema"
    - from: "packages/domain/src/runs/schema.ts"
      to: "packages/domain/src/common/schemaVersion.ts"
      via: "run records use the shared schema-version contract"
      pattern: "schemaVersion"
    - from: "packages/domain/src/index.ts"
      to: "packages/domain/src/scenario/schema.ts"
      via: "domain package exports the scenario contract for downstream plans"
      pattern: "scenarioDocumentSchema"
---

<objective>
Implement the core Phase 1 domain contract foundations: shared schema-version/provenance utilities, the scenario schema, the default scenario factory, and the base run-record schema.

Purpose: This plan creates the minimal contract surface that the later preset, persistence, and workspace plans will consume without inventing their own ad hoc shapes.

Output: A tested `@f1-modeling/domain` core that validates scenario documents and run-record structure.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@.planning/phases/01-foundations-and-scenario-schema/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared schema-version and provenance foundations plus the scenario contract</name>
  <files>packages/domain/src/common/schemaVersion.ts, packages/domain/src/common/provenance.ts, packages/domain/src/scenario/schema.ts, packages/domain/src/scenario/defaultScenario.ts, packages/domain/src/index.ts</files>
  <action>
    1. Use `zod` as the runtime-validation source for the domain package and export inferred TypeScript types from the shared package entrypoint.

    2. Implement shared `schemaVersion` and provenance helpers first so every later document family can reuse:
       - explicit `schemaVersion`
       - identity/timestamp metadata where appropriate
       - `sourceType` values `documented-fact | engineering-inference | placeholder`

    3. Implement `scenarioDocumentSchema` with the exact Phase 1 boundary from `01-CONTEXT.md`:
       - scenario identity and notes
       - circuit
       - session preset reference
       - regulation preset reference
       - weather preset reference
       - driver profile placeholder data
       - reproducibility seed
       - assumption notes
       - reserved placeholder sections for future observer config, controller config, and imported-data alignment

    4. Keep those future-facing sections explicit placeholders only. Do not encode EKF parameters, MPC horizons, telemetry details, or subsystem dynamics here.

    5. Implement `createDefaultScenario()` so later API/UI code can start from one canonical factory.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/domain
  </verify>
  <done>The domain package exports shared schema-version/provenance helpers, a validated ScenarioDocument schema, and a canonical default scenario factory.</done>
</task>

<task type="auto">
  <name>Task 2: Define the base run-record schema and core domain tests</name>
  <files>packages/domain/src/runs/schema.ts, packages/domain/src/index.ts, packages/domain/src/__tests__/documents.test.ts</files>
  <action>
    1. Implement `runRecordSchema` with the reproducibility fields Phase 1 requires:
       - `runId`
       - `createdAt`
       - `scenarioId`
       - `scenarioSnapshot`
       - `presetSnapshots`
       - `modelVersion`
       - `appVersion`
       - `seed`
       - `status`
       - `summaryMetrics`
       - `artifacts`
       - `assumptionNotes`

    2. This plan defines the shape only. Append-only run construction and preset resolution happen in Plan 01-05.

    3. Create `documents.test.ts` with schema-level coverage for valid and invalid scenario/run payloads plus the rule that placeholder extension points remain explicit instead of hidden.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run test --workspace @f1-modeling/domain -- --run
  </verify>
  <done>The base run-record schema is validated in the shared domain package and core contract tests pass.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace @f1-modeling/domain` passes.
- `npm run test --workspace @f1-modeling/domain -- --run` passes for the core schema suite.
</verification>

<success_criteria>
- Scenario and run contracts exist as separate, versioned shared types.
- Provenance and placeholder-extension rules are enforced in the shared domain package.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-04-SUMMARY.md`
</output>
