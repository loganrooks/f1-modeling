---
phase: 01-foundations-and-scenario-schema
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/domain/package.json
  - packages/domain/tsconfig.json
  - packages/domain/src/index.ts
  - packages/visuals/package.json
  - packages/visuals/tsconfig.json
  - packages/visuals/src/index.ts
  - packages/sim-core/package.json
  - packages/sim-core/tsconfig.json
  - packages/sim-core/src/contracts.ts
  - packages/sim-core/src/phase1Placeholder.ts
  - packages/sim-core/src/index.ts
  - workspace-data/scenarios/.gitkeep
  - workspace-data/runs/.gitkeep
autonomous: true
requirements: [PLAT-01, DESN-02]

must_haves:
  truths:
    - "The repo has separate shared packages for domain contracts, reusable visuals, and simulation seams."
    - "The simulation seam is an explicit deterministic placeholder harness, not a real lap model and not a hidden app-state shortcut."
    - "The canonical on-disk directories for saved scenarios and runs exist before API persistence is implemented."
    - "The shared package seams are established without forcing root-level install or typecheck coupling to app-shell creation in the same wave."
  artifacts:
    - path: "packages/sim-core/src/contracts.ts"
      provides: "Phase 1 simulation interfaces for later placeholder run execution"
      exports: ["SimulationRunRequest", "SimulationRunSummary", "SimulationHarness"]
    - path: "packages/sim-core/src/phase1Placeholder.ts"
      provides: "deterministic placeholder run harness"
      exports: ["runPhase1PlaceholderScenario"]
    - path: "packages/domain/src/index.ts"
      provides: "domain package entrypoint placeholder for later schema exports"
      contains: "export"
    - path: "packages/visuals/src/index.ts"
      provides: "visuals package entrypoint placeholder for later UI primitive exports"
      contains: "export"
    - path: "workspace-data/scenarios/.gitkeep"
      provides: "tracked scenarios directory for file-backed persistence"
      contains: ""
  key_links:
    - from: "packages/sim-core/src/index.ts"
      to: "packages/sim-core/src/contracts.ts"
      via: "sim-core entrypoint exports Phase 1 run contracts"
      pattern: "SimulationRunRequest"
    - from: "packages/sim-core/src/index.ts"
      to: "packages/sim-core/src/phase1Placeholder.ts"
      via: "sim-core entrypoint exports the deterministic placeholder harness"
      pattern: "runPhase1PlaceholderScenario"
    - from: "package.json"
      to: "packages/domain/package.json"
      via: "root workspace globs already include the shared package directories"
      pattern: "packages/*"
---

<objective>
Create the shared package seams and persistence directories that the rest of Phase 1 will build on.

Purpose: This plan separates reusable package boundaries and the Phase 1 placeholder simulation seam from the app-shell scaffolding, keeping the architecture layered and honest.

Output: Linked `packages/domain`, `packages/visuals`, and `packages/sim-core` workspaces plus tracked `workspace-data/` directories.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@.planning/phases/01-foundations-and-scenario-schema/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared workspace package shells</name>
  <files>packages/domain/package.json, packages/domain/tsconfig.json, packages/domain/src/index.ts, packages/visuals/package.json, packages/visuals/tsconfig.json, packages/visuals/src/index.ts, packages/sim-core/package.json, packages/sim-core/tsconfig.json, packages/sim-core/src/index.ts</files>
  <action>
    1. Scaffold `packages/domain`, `packages/visuals`, and `packages/sim-core` as TypeScript workspaces with stable package names and local `build`, `test`, and `typecheck` scripts.

    2. Keep `packages/domain/src/index.ts` and `packages/visuals/src/index.ts` as entrypoint placeholders only in this plan. Do not start implementing schemas or reusable UI primitives yet.

    3. `packages/sim-core/src/index.ts` should export the simulation contract and placeholder harness surfaces that later plans will rely on.

    4. Do not rerun root `npm install` in this plan. Keep this plan wave-safe with 01-02 by limiting verification to the shared packages only. Root-level workspace relinking can happen later when a plan actually needs cross-workspace package resolution.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/domain --workspace @f1-modeling/visuals --workspace @f1-modeling/sim-core
  </verify>
  <done>The shared packages exist and typecheck as stable seams without forcing root-level install coupling to the app-shell plan in the same wave.</done>
</task>

<task type="auto">
  <name>Task 2: Define the deterministic Phase 1 simulation seam and local data directories</name>
  <files>packages/sim-core/src/contracts.ts, packages/sim-core/src/phase1Placeholder.ts, packages/sim-core/src/index.ts, workspace-data/scenarios/.gitkeep, workspace-data/runs/.gitkeep</files>
  <action>
    1. Implement `packages/sim-core/src/contracts.ts` with the small Phase 1 run surface only:
       - `SimulationRunRequest`
       - `SimulationRunSummary`
       - `SimulationHarness`

    2. Implement `runPhase1PlaceholderScenario(request)` in `phase1Placeholder.ts`. It must be deterministic and clearly labeled as a placeholder harness for workspace wiring only.

    3. The placeholder harness may emit a few summary metrics and a trivial trace shape for UI wiring, but it must not implement lap physics, stint dynamics, environment coupling, observer logic, control logic, telemetry import, or RL scaffolding.

    4. Create tracked `workspace-data/scenarios/` and `workspace-data/runs/` directories with `.gitkeep` files so later persistence work writes to inspectable on-disk locations.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run typecheck --workspace @f1-modeling/sim-core && test -d workspace-data/scenarios && test -d workspace-data/runs
  </verify>
  <done>The shared package seams compile, the deterministic placeholder harness exists, and the on-disk persistence directories are present.</done>
</task>

</tasks>

<verification>
- `npm run typecheck --workspace @f1-modeling/domain --workspace @f1-modeling/visuals --workspace @f1-modeling/sim-core` passes.
- `npm run typecheck --workspace @f1-modeling/sim-core` passes after the placeholder harness is added.
- `workspace-data/scenarios/` and `workspace-data/runs/` exist on disk.
</verification>

<success_criteria>
- Shared package boundaries are explicit before schema, persistence, or workspace assembly work starts.
- The Phase 1 simulation seam is honest and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-03-SUMMARY.md`
</output>
