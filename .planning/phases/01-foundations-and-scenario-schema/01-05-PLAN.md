---
phase: 01-foundations-and-scenario-schema
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - package-lock.json
  - packages/domain/package.json
  - packages/domain/src/presets/schema.ts
  - packages/domain/src/presets/registry.ts
  - packages/domain/src/node/presetCatalog.ts
  - packages/domain/src/runs/createRunRecord.ts
  - packages/domain/src/index.ts
  - packages/domain/src/__tests__/documents.test.ts
  - presets/regulations/fia-2026-baseline.json
  - presets/sessions/grand-prix-race.json
  - presets/sessions/qualifying-session.json
  - presets/weather/dry-baseline.json
  - presets/weather/light-rain-placeholder.json
autonomous: true
requirements: [PLAT-03, VAL-02, DESN-02]

must_haves:
  truths:
    - "Preset documents load from disk as a separate validated contract family with explicit provenance."
    - "The preset catalog includes regulation, session, and weather documents that Phase 1 can select without hard-coded UI constants."
    - "Disk loading for presets stays on a node-only surface so the package root remains safe to import from browser code."
    - "Run-record creation embeds both stable references and resolved snapshots of the scenario and presets used at execution time."
    - "Append-only run-history construction is centralized in one shared helper rather than being reimplemented in API or UI code."
  artifacts:
    - path: "packages/domain/src/presets/schema.ts"
      provides: "preset document schema family with preset type and provenance"
      exports: ["presetDocumentSchema", "PresetDocument", "PresetType"]
    - path: "packages/domain/src/presets/registry.ts"
      provides: "browser-safe preset catalog helpers that operate on already-loaded documents"
      exports: ["groupPresetCatalog", "getPresetById"]
    - path: "packages/domain/src/node/presetCatalog.ts"
      provides: "node-only filesystem loader for the preset catalog"
      exports: ["loadPresetCatalogFromDisk"]
    - path: "packages/domain/src/runs/createRunRecord.ts"
      provides: "append-only run-record construction helper"
      exports: ["createRunRecord"]
    - path: "presets/regulations/fia-2026-baseline.json"
      provides: "initial regulation preset document"
      contains: "\"presetType\": \"regulation\""
    - path: "packages/domain/src/__tests__/documents.test.ts"
      provides: "extended domain tests for presets and run-record snapshots"
      min_lines: 140
  key_links:
    - from: "packages/domain/src/node/presetCatalog.ts"
      to: "presets/regulations/fia-2026-baseline.json"
      via: "catalog loading validates regulation presets from disk"
      pattern: "fia-2026-baseline"
    - from: "packages/domain/src/runs/createRunRecord.ts"
      to: "packages/domain/src/runs/schema.ts"
      via: "run construction materializes the shared run-record schema"
      pattern: "runRecordSchema"
    - from: "packages/domain/src/runs/createRunRecord.ts"
      to: "packages/domain/src/scenario/schema.ts"
      via: "run records embed validated scenario snapshots"
      pattern: "scenarioSnapshot"
    - from: "packages/domain/package.json"
      to: "packages/domain/src/node/presetCatalog.ts"
      via: "node-only subpath export keeps disk loading off the browser-safe package root"
      pattern: "node/preset-catalog"
---

<objective>
Add the preset contract family, seed the Phase 1 preset catalog, and implement append-only run-record construction with embedded snapshots.

Purpose: This plan closes the schema gap between editable scenarios and reproducible runs, while keeping regulation/session/weather logic data-driven and provenance-aware.

Output: Browser-safe preset schemas/helpers, a node-only disk loader, seeded preset documents on disk, and a shared helper that builds immutable, reproducible run records.
</objective>

<execution_context>
@/home/rookslog/.codex/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.codex/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-and-scenario-schema/01-CONTEXT.md
@.planning/phases/01-foundations-and-scenario-schema/01-RESEARCH.md
@.planning/phases/01-foundations-and-scenario-schema/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the preset schema family and seed the Phase 1 catalog</name>
  <files>packages/domain/package.json, packages/domain/src/presets/schema.ts, packages/domain/src/presets/registry.ts, packages/domain/src/node/presetCatalog.ts, packages/domain/src/index.ts, presets/regulations/fia-2026-baseline.json, presets/sessions/grand-prix-race.json, presets/sessions/qualifying-session.json, presets/weather/dry-baseline.json, presets/weather/light-rain-placeholder.json</files>
  <action>
    1. Implement `presetDocumentSchema` with shared provenance fields and explicit preset categories for Phase 1:
       - `regulation`
       - `session`
       - `weather`

    2. Create the seed preset documents from research guidance:
       - `presets/regulations/fia-2026-baseline.json`
       - `presets/sessions/grand-prix-race.json`
       - `presets/sessions/qualifying-session.json`
       - `presets/weather/dry-baseline.json`
       - `presets/weather/light-rain-placeholder.json`

    3. Every preset document must declare provenance and `sourceType` honestly. If a field is inferred or placeholder, mark it explicitly rather than implying official fidelity.

    4. Keep the package root browser-safe:
       - `packages/domain/src/presets/registry.ts` should expose pure helpers that operate on already-loaded preset documents
       - `packages/domain/src/node/presetCatalog.ts` should expose `loadPresetCatalogFromDisk(...)` for node-side filesystem loading only
       - `packages/domain/package.json` should expose a node-only subpath such as `@f1-modeling/domain/node/preset-catalog` rather than re-exporting disk loading from the package root

    5. After changing the domain package exports, rerun root `npm install` so the workspace lockfile and package links resolve the node-only subpath for downstream plans and verification commands.

    6. Later browser code must receive preset data through the local API, not by importing the node-only loader.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npx tsx --eval "import { loadPresetCatalogFromDisk } from '@f1-modeling/domain/node/preset-catalog'; const catalog = loadPresetCatalogFromDisk('/home/rookslog/workspace/projects/f1-modeling/presets'); if (!catalog.regulation.length || !catalog.session.length || !catalog.weather.length) throw new Error('preset catalog incomplete');"
  </verify>
  <done>The preset catalog loads from disk through a node-only helper, the package root stays browser-safe, and regulation/session/weather presets are available with explicit provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Implement append-only run-record construction and extend the domain tests</name>
  <files>packages/domain/src/runs/createRunRecord.ts, packages/domain/src/index.ts, packages/domain/src/__tests__/documents.test.ts</files>
  <action>
    1. Implement `createRunRecord(...)` as the only sanctioned Phase 1 helper for turning a validated scenario, resolved preset documents, placeholder run output, and version metadata into an immutable run record.

    2. `createRunRecord(...)` must embed both:
       - stable ids/references for the originating scenario and presets
       - resolved snapshots of the effective scenario and presets used when the run was created

    3. Preserve the reproducibility fields from Plan 01-04 and ensure the helper remains append-only. Do not let API or UI code mutate historical run documents in place.

    4. Extend `documents.test.ts` to cover preset validation, node-only catalog loading through the package subpath, run-record snapshot embedding, and invalid run construction paths.
  </action>
  <verify>
    cd /home/rookslog/workspace/projects/f1-modeling && npm run test --workspace @f1-modeling/domain -- --run
  </verify>
  <done>The domain package can build immutable run records with both references and snapshots, and the full domain test suite passes.</done>
</task>

</tasks>

<verification>
- `npx tsx --eval "import { loadPresetCatalogFromDisk } from '@f1-modeling/domain/node/preset-catalog'; ..."` succeeds for the seeded preset catalog.
- `npm run test --workspace @f1-modeling/domain -- --run` passes with preset and run-record coverage.
</verification>

<success_criteria>
- Regulation, session, and weather presets exist as validated documents on disk.
- Run-record creation is centralized, append-only, and snapshot-aware.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-and-scenario-schema/01-05-SUMMARY.md`
</output>
